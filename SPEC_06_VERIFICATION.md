---
id: SPEC_06
title: 레벨·점수·생성기 자동 검증 보강
depends_on: [SPEC_01, SPEC_03, SPEC_05]
max_iterations: 3
---

# SPEC 06 — 레벨·점수·생성기 자동 검증 보강

> **선행 조건 주의 — 확인 백로그 55건**
> 여섯 개 SPEC이 쌓이는 동안 브라우저 확인은 SPEC_00 정적 화면 한 번뿐이었다. 이 SPEC은
> `test.html`에서 결과를 봐야 값을 하므로, 밀린 확인을 먼저 끝내는 것을 강하게 권장한다.

## 1. 목표

레벨 계산, 낙하 간격 하한, 점수 배수 적용 범위, 조각 생성기의 결정론을 `test.js`에서
검증한다. **이 SPEC은 기능을 만들지 않는다.** 게임 동작을 바꾸지 않고 검증만 보강한다.

이미 검증된 것과 새로 얻는 것을 구분해 둔다.

| 요구사항 | 현재 상태 |
| --- | --- |
| 제거 줄 `0`, `9`, `10`, `19`의 레벨 | `0`~`9`는 SPEC_05에서 확인됨. **`10`, `19`가 새로움** |
| 높은 레벨의 `100ms` 하한 | SPEC_05에서 레벨 `11`~`999` 확인됨. **줄 수에서 출발하는 경로가 새로움** |
| 줄 점수만 레벨 배수 | SPEC_05에서 확인됨. **회귀 방어로 유지** |
| 고정 난수 결정론 | **전부 새로움** |

`max_iterations`는 기본 `3`이다. 산출물이 파일 하나이고 대부분 AUTO다.

## 2. 기술과 파일 구조

- `CLAUDE.md`의 기술 제약을 그대로 따른다. **파일을 추가하지 않는다.**
- **이 프로젝트의 첫 검증 전용 SPEC이다.** 바뀌는 파일은 `test.js` 하나뿐이다.

| 파일 | 이번 SPEC에서 하는 일 |
| --- | --- |
| `test.js` | 테스트 케이스 추가 |
| `game.js` | **변경 없음** |
| `main.js` | **변경 없음** |
| `index.html` | **변경 없음** |
| `style.css` | **변경 없음** |
| `test.html` | **변경 없음** |

고정 난수 생성기는 `test.js` 안에서만 만든다. `game.js`에 공개 함수로 넣지 않는다.
검증만 하는 SPEC에 기능을 들이지 않기 위해서다.

## 3. 화면 정의

**이번 SPEC에서 화면은 바뀌지 않는다.** `index.html`의 구성, D5(중앙 배치·네온),
D6(로고 점멸), D7(HUD 레벨) 모두 그대로다.

`test.html`의 결과 목록에 이번에 추가한 케이스가 줄로 늘어나고, 상단 요약의 전체 개수가
그만큼 커진다. 이것 말고 보이는 변화는 없다.

## 4. 상태와 공개 함수

**이번 SPEC에서 변경 없음.** `globalThis.TetrisGame`에 값이나 함수를 더하거나 빼지 않고,
상태 필드도 그대로 여섯 개(`board`/`score`/`lines`/`status`/`piece`/`randomizer`)다.

검증에 쓰는 도우미는 전부 `test.js` 안의 지역 함수다.

### 4-1. 테스트 안에서 만드는 도우미

| 이름 | 하는 일 |
| --- | --- |
| `previewableRandomizer(list)` | `{ next, peek }`를 돌려준다. `peek()`는 다음에 나올 종류를 **소비하지 않고** 알려주고, `next()`는 소비하며 돌려준다 |
| `seededRandomizer(seed)` | 정수 시드로 결정론적 순서를 만드는 함수. 같은 시드는 항상 같은 순서를 낸다 |

`seededRandomizer`는 선형 합동 생성기(LCG)를 쓴다. `Math.random()`을 쓰면 결정론 검증이
성립하지 않는다.

## 5. 범위 밖

- 다음 조각 미리보기 **기능**과 그 화면 요소
- `game.js`에 시드 생성기를 공개 함수로 추가하는 것
- 게임 로직·화면·스타일 변경
- 성능 측정과 벤치마크
- 브라우저 자동화 도구 도입
- 테스트 러너 교체나 외부 테스트 프레임워크
- 커버리지 측정
- 레벨 상한, 콤보, T-스핀

## 6. 완료 조건

- [ ] `levelFor(0)`이 `1`이다. (AUTO)
- [ ] `levelFor(9)`가 `5`다. (AUTO)
- [ ] `levelFor(10)`이 `6`다. (AUTO)
- [ ] `levelFor(19)`가 `10`이다. (AUTO)
- [ ] 레벨 `10`의 낙하 간격이 `160`ms다. (AUTO)
- [ ] 레벨 `11`의 낙하 간격이 `100`ms다. (AUTO)
- [ ] 레벨 `12`부터 `999`까지 낙하 간격이 모두 `100`ms다. (AUTO)
- [ ] 제거 줄 `19`에서 출발한 낙하 간격이 `160`ms다. (AUTO)
- [ ] 제거 줄 `20`에서 출발한 낙하 간격이 `100`ms다. (AUTO)
- [ ] 줄 점수에 레벨 배수가 곱해진다. (AUTO)
- [ ] 소프트 드롭 점수에 레벨 배수가 곱해지지 않는다. (AUTO)
- [ ] 하드 드롭 점수에 레벨 배수가 곱해지지 않는다. (AUTO)
- [ ] 한 착지에서 드롭 점수는 그대로이고 줄 점수만 배수를 받는다. (AUTO)
- [ ] `peek()`이 알려준 종류가 실제로 생성되는 조각과 같다. (AUTO)
- [ ] 조각을 생성한 뒤 `peek()`이 그다음 종류로 바뀐다. (AUTO)
- [ ] 같은 시드로 만든 생성기 두 개가 같은 순서를 낸다. (AUTO)
- [ ] 생성기를 소비한 뒤 같은 시드로 새로 만들면 처음 순서부터 다시 나온다. (AUTO)
- [ ] `test.html`을 브라우저에서 직접 열었을 때 실패가 `0`이다. (HUMAN)
- [ ] `test.html`의 전체 케이스 수가 이전보다 늘어난 것이 보인다. (HUMAN)
- [ ] `index.html`을 브라우저에서 직접 열었을 때 콘솔 오류가 없다. (HUMAN)

AUTO 17개, HUMAN 3개, 합계 20개.

## 7. 자동 검증

`test.js`에 케이스를 추가한다. 도우미는 다음 형태로 만든다.

```js
function previewableRandomizer(list) {
  var index = 0;
  return {
    peek: function () {
      return list[index % list.length];
    },
    next: function () {
      var value = list[index % list.length];
      index++;
      return value;
    }
  };
}

function seededRandomizer(seed) {
  var value = seed;
  return function () {
    value = (value * 1103515245 + 12345) % 2147483648;
    return TetrisGame.TETROMINO_TYPES[value % TetrisGame.TETROMINO_TYPES.length];
  };
}
```

### 7-1. 레벨 표

| 제거 줄 | 기대 레벨 | 기대 간격 |
| --- | --- | --- |
| `0` | `1` | `700`ms |
| `9` | `5` | `460`ms |
| `10` | `6` | `400`ms |
| `19` | `10` | `160`ms |
| `20` | `11` | `100`ms |
| `40` | `21` | `100`ms |

### 7-2. 점수 배수

- `lines`가 `2`(레벨 2)인 상태에서 한 줄을 지우면 `score`가 `200` 는다.
- `lines`가 `18`(레벨 10)인 상태에서 한 줄을 지우면 `score`가 `1000` 는다.
- `lines`가 `18`인 상태에서 소프트 드롭 다섯 번은 `5`점이다.
- `lines`가 `18`인 상태에서 하드 드롭은 칸수 × `2`점이다.
- 하드 드롭으로 `d`칸 내려가 한 줄을 지우면 `2 × d + 100 × 레벨`이다.

### 7-3. 생성기 결정론

`미리보기 → 실제 실행 → 새 미리보기` 순서를 그대로 확인한다.

1. `previewableRandomizer(['T', 'O', 'I'])`를 만든다.
2. `peek()`이 `'T'`인지 본다. (미리보기)
3. `createInitialState({ randomizer: r.next })`로 상태를 만들고 `spawnPiece()`를 부른다.
4. 생성된 `piece.type`이 `'T'`인지 본다. (실제 실행)
5. `peek()`이 이제 `'O'`인지 본다. (새 미리보기)
6. 다시 생성해 `'O'`가 나오고 `peek()`이 `'I'`가 되는지 본다.
7. 같은 시드로 `seededRandomizer`를 둘 만들어 스무 번씩 뽑은 순서가 같은지 본다.
8. 한쪽을 스무 번 소비한 뒤 같은 시드로 새로 만들어 첫 값부터 같은지 본다.

## 8. 수동 검증

1. `test.html`을 브라우저에서 직접 연다.
2. 요약 줄의 **실패 개수가 `0`**인지 확인한다.
3. 전체 케이스 수가 이전(132개)보다 늘어난 것을 확인한다.
4. 목록에서 `FAIL`로 시작하는 줄이 하나도 없는지 확인한다.
5. `index.html`을 열어 콘솔 오류가 없는지 확인한다.
6. `시작`을 눌러 게임이 전과 똑같이 동작하는지 확인한다. 이 SPEC은 게임을 바꾸지 않았으므로
   달라진 점이 보이면 그 자체가 실패다.

## 9. 안전과 정지 조건

- 프로젝트 폴더 밖 파일을 만들거나 수정하지 않는다.
- 외부 배포와 유료 서비스를 사용하지 않는다.
- 같은 실패가 두 번 반복되면 중단한다.
- 이 문서에 없는 화면·기술 결정을 해야 하면 추측하지 않고 `[사람 확인 필요]`로 보고한다.

### 이 SPEC 고유의 항목

- **`test.js` 말고는 아무 파일도 고치지 않는다.** 이것이 이 SPEC의 정체성이다.
  `/spec-run` 4단계에서 `game.js`, `main.js`, `index.html`, `style.css`, `test.html`의
  해시가 회차 시작 전과 같아야 한다.
- **테스트가 실패하면 `game.js`를 고치지 않는다.** 검증만 하는 SPEC이 대상 코드를 함께
  고치면 "테스트가 통과하도록 구현을 맞추는" 일이 되어 검증의 의미가 사라진다. 실패하면
  이렇게 한다.
  1. 어느 SPEC의 어느 계약이 깨졌는지 특정한다.
  2. `MEMORY.md`의 `반복된 실패`에 기록한다.
  3. 사용자에게 보고하고 **정지**한다. 수정은 해당 SPEC의 회차에서 한다.
- **테스트를 약하게 쓰지 않는다.** 목적은 통과가 아니라 결함을 찾는 것이다. 기대값을
  실제 출력에 맞춰 고쳐 통과시키는 일을 하지 않는다. §7-1과 §7-2의 표에 적힌 수는
  요구사항 공식에서 나온 값이며, 구현이 그와 다르면 구현이 틀린 것이다.
- **`Math.random()`을 결정론 검증에 쓰지 않는다.** 시드 생성기만 쓴다. 무작위에 의존하는
  테스트는 가끔 실패해 `반복된 실패` 판정을 오염시킨다.
- **기존 케이스를 지우거나 약화시키지 않는다.** 이번 회차는 추가만 한다. 기존 132개가
  그대로 남아 있어야 한다.
- **회차 예산.** 완료 조건 20개지만 AUTO가 17개다. 회차 2를 마친 시점에 `미확인` 또는
  `실패`가 3건 이상이면 사용자에게 보고하고 정지한다.
- **확인 백로그.** 앞선 SPEC의 미확인 55건이 남아 있다. SPEC_06 회차 1의 사람 확인 요청은
  이것들과 합쳐서 한 번에 낸다.
