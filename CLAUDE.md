# Loop Engineering — 프로젝트 규칙

이 문서는 모든 실행에서 항상 적용된다. SPEC 문서와 충돌하면 **SPEC이 우선**하고,
그 사실을 보고서에 명시한다.

작업 흐름은 두 개의 커맨드로만 이루어진다.

- `/spec-new <요구사항>` — 요구사항을 SPEC 초안 한 장으로 바꾼다. 코드는 쓰지 않는다.
- `/spec-run <SPEC 파일>` — SPEC 한 장을 실행하고 검증하고 보고하고 `MEMORY.md`를 갱신한다.

산출물 코드는 `tetris-loop-workshop/` 안에만 만든다.

---

## 1. 기술 제약

- **HTML5, CSS, 브라우저 기본 JavaScript만** 사용한다.
- 다음을 금지한다.
  - 프레임워크, 번들러, 트랜스파일러, 패키지 매니저
  - 외부 라이브러리, CDN `<script>`/`<link>`, 웹폰트 원격 로드
  - 런타임 네트워크 요청 (`fetch`, `XMLHttpRequest`, WebSocket)
  - `package.json`, `node_modules`, 설정 파일, 개발 서버, 빌드 명령
  - ES 모듈 문법 (`import` / `export`), CommonJS (`require(` / `module.exports`)
- `index.html`은 `style.css`, `game.js`, `main.js`를 **직접** 불러온다.
  `game.js`가 `main.js`보다 먼저 오고, 두 스크립트에는 `defer`를 쓴다.
- `test.html`은 `game.js`, `test.js`를 직접 불러온다.
- 공개 API는 `globalThis.TetrisGame`에 붙인다.
- 산출물은 SPEC이 명시한 파일만 존재한다. SPEC_00 기준 6개:
  `index.html`, `style.css`, `game.js`, `main.js`, `test.html`, `test.js`.
  파일을 더 만들어야 할 것 같으면 만들지 말고 `[사람 확인 필요]`로 보고한다.

**하네스 도구와 산출물을 구분한다.** 검증할 때 로컬 `node`를 쓰는 것은 허용된다.
이것은 하네스 도구이지 산출물의 의존성이 아니다. 산출물 6개 파일 안에는 node 전용
코드가 절대 들어가지 않는다.

## 2. 폴더 경계

프로젝트 루트는 `C:\my_program\Loop-Engineering` 이다.

**쓰기 허용**

| 경로 | 조건 |
| --- | --- |
| `tetris-loop-workshop\**` | 산출물. `/spec-run` 중에만 |
| `MEMORY.md` | 에이전트만 갱신 |
| `SPEC_*.md` (새 파일) | `/spec-new` 중에만 |
| 세션 스크래치패드 | 임시 검사 스크립트, 중간 출력 |

**쓰기 금지**

- `CLAUDE.md`
- `.claude\commands\**`
- 이미 존재하는 `SPEC_*.md`의 본문 (완료 조건 체크박스 포함 — 진행 상태는 `MEMORY.md`에 기록한다)
- 프로젝트 루트 밖의 모든 경로

**행위 금지**

- 전역 설치 (`npm i -g`, `pip install`, 패키지 매니저 전반)
- 환경 변수·레지스트리·시스템 설정 변경
- 외부 배포, 유료 서비스, 원격 푸시
- 임시 파일을 프로젝트 폴더 안에 남기는 것

## 3. 정지 조건

아래 중 하나라도 해당하면 **즉시 멈춘다**. 코드는 마지막 정상 상태로 남기고,
`MEMORY.md`에 어느 조건에 걸렸는지 기록하고, 사용자에게 보고한다.

- **(a) 회차 소진** — 대상 SPEC frontmatter의 `max_iterations`에 도달했다.
- **(b) 반복 실패** — 동일한 실패 시그니처가 2회 발생했다.
  시그니처 = `완료 조건 번호 + 증상 한 줄`. `MEMORY.md`의 `반복된 실패` 표로 판정한다.
- **(c) 명세에 없는 결정** — SPEC에 없는 화면·기술 결정이 필요하다.
  추측하지 말고 `[사람 확인 필요]`로 보고한다.
- **(d) 범위 밖 침범** — SPEC §5 "범위 밖" 항목을 구현해야만 완료할 수 있다.
- **(e) 경계 이탈** — 위 폴더 경계를 벗어나야 진행할 수 있다.

정지는 실패가 아니다. 정지 사유를 정확히 남기는 것이 이번 실행의 산출물이다.
정지 조건에 걸린 상태에서 "우회해서 어떻게든 완료"를 시도하지 않는다.

## 4. 완료 선언의 근거 규칙

### 근거의 종류

완료 조건 한 줄마다 근거를 하나 붙인다. 근거는 두 종류뿐이다.

- **`AUTO`** — 이번 실행에서 실제로 돌린 명령과 그 출력.
  출력을 그대로 인용하지 못하면 `AUTO`가 아니다. 기억이나 추론은 근거가 아니다.
- **`HUMAN`** — 사람이 프롬프트로 전달한 확인 결과.
  `MEMORY.md`의 `사람 확인 기록`에 날짜와 원문이 남아 있어야 한다.

근거가 없으면 상태는 `미확인`이다.

### HUMAN으로만 인정하는 것

브라우저 없이는 확인할 수 없다. 에이전트가 통과 판정을 내리지 않는다.

- `index.html`을 열었을 때 콘솔 오류가 없음
- `test.html`의 화면 표시 결과 (모든 테스트 통과 여부)
- 실제로 렌더링된 텍스트·레이아웃·셀 개수
- 브라우저 폭 390px에서 가로 스크롤 없음
- 5초 대기·방향키 입력 후 보드가 변하지 않음

에이전트는 이 항목들에 대해 코드를 근거로 "통과"라고 쓸 수 없다.
쓸 수 있는 것은 `미확인 — 사람 확인 대기` 뿐이다.

### 완료 선언

- 완료 조건 전부가 `통과(AUTO)` 또는 `통과(HUMAN)`일 때만 "완료"라고 쓴다.
- `미확인`이나 `실패`가 하나라도 있으면 "완료", "다 됐습니다", "구현 끝" 같은 표현을
  쓰지 않는다. 남은 항목 수를 명시한다.

### 금지 표현

- "정상 동작할 것으로 보입니다", "문제 없을 것입니다", "아마 통과합니다"
- "테스트를 통과했습니다" — 실제로 돌린 출력이 없는 경우
- 에이전트 추정과 사람 확인을 같은 줄에 섞어 쓰는 것

확인하지 않은 것은 확인하지 않았다고 쓴다. 이것이 이 프로젝트에서 가장 중요한 규칙이다.
