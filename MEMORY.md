# MEMORY — 실행 간 상태

## 갱신 규칙

- 이 문서는 **에이전트만** 갱신한다. `/spec-run`의 7단계에서만 쓴다.
- 사람은 이 파일을 직접 편집하지 않는다. 브라우저에서 확인한 결과는 **프롬프트로 전달**하고,
  에이전트가 `사람 확인 기록`에 `[YYYY-MM-DD] (사람) <원문>` 형식으로 옮겨 적는다.
- 사람이 확인한 것과 에이전트가 추정한 것을 같은 줄에 섞지 않는다.
- 섹션을 지우거나 순서를 바꾸지 않는다. 내용이 없으면 `(없음)`이라고 쓴다.
- 근거 규칙은 `CLAUDE.md` §4를 따른다.

---

## 현재 상태

| 항목 | 값 |
| --- | --- |
| 활성 SPEC | `SPEC_05_LEVELS.md` |
| 회차 | 1 / 4 |
| 상태 | 진행중 — 사람 확인 대기 |
| 마지막 갱신 | 2026-09-10 (SPEC_05 회차 1 · `/spec-run`) |
| 산출물 폴더 | `tetris-loop-workshop/` (6개 파일 존재) |
| 이전 SPEC | `SPEC_00` 완료(단 D5로 5건 재확인 필요) · `SPEC_01` AUTO 통과, HUMAN 11건 미확인 |

> `확정된 계약`의 사전 확정 결정(D1~D4)과 검증 규칙 R1은 `/spec-run` 밖에서 **사람 지시**로
> 회차 0에 기록되었다. 검증 결과가 아니라 결정이다.

---

## 파일 인벤토리

| 파일 | 역할 | 존재 |
| --- | --- | --- |
| `tetris-loop-workshop/index.html` | 게임 화면 | ✓ |
| `tetris-loop-workshop/style.css` | 스타일 | ✓ |
| `tetris-loop-workshop/game.js` | 상태·공개 API | ✓ |
| `tetris-loop-workshop/main.js` | 화면 조립 | ✓ |
| `tetris-loop-workshop/test.html` | 브라우저 테스트 페이지 | ✓ |
| `tetris-loop-workshop/test.js` | 테스트 러너·케이스 (00 15 + 01 21 + 02 30 + 03 22 + 04 30 + 05 14 = 132) | ✓ |

회차 1 · 5-1 출력 기준. 산출물 폴더에 이 6개 외의 파일은 없다.

하네스 파일: `CLAUDE.md`, `MEMORY.md`, `.claude/commands/spec-new.md`,
`.claude/commands/spec-run.md`, `SPEC_00_PROJECT.md` — 산출물 6개 파일 계산에서 제외한다.

---

## 확정된 계약

SPEC이 지정하지 않아 이 프로젝트에서 정한 것을 기록한다.
**한 번 기록되면 이후 회차에서 바꾸지 않는다.**

### 이름 계약

| 대상 | 확정된 이름 | 정한 회차 |
| --- | --- | --- |
| `createInitialState()` 반환 필드 | `board`, `score`, `lines`, `status` | 1 |
| 빈 셀 값 | `0` (SPEC §4 명시) | - |
| `GAME_STATUS` 키 | 키와 값이 같은 문자열 (`GAME_STATUS.READY === 'READY'`) | 0 · 사람 지시 |

`GAME_STATUS` 행의 이유: SPEC §4는 "값을 가진 상수 객체"라고만 해서 키 이름이 미지정인데,
`spec-run.md` 5-5의 `contract-check.cjs`는 이미 키=값을 전제로 검사한다. 가정을 규칙으로
승격시켜 검사 스크립트와 SPEC 해석을 일치시킨다.

SPEC이 지정한 것 (변경 불가): `globalThis.TetrisGame`, `BOARD_WIDTH`, `BOARD_HEIGHT`,
`GAME_STATUS`, `createEmptyBoard()`, `createInitialState()`.

### 사전 확정 결정 (회차 0 · 사람 지시 · 2026-09-10)

SPEC_00 리뷰에서 드러난 공백을 메우기 위해 사람이 지시한 결정이다.
`SPEC_00_PROJECT.md`는 수정하지 않으며, 해석은 이 표를 따른다.

| ID | 결정 | 근거가 된 SPEC 공백 |
| --- | --- | --- |
| D1 | 명시되지 않은 **시각 속성**(색상, 셀 크기, 셀 간격, 테두리, 배경, 폰트, 여백, 패널 폭)은 구현자가 정한다. 단 §3에 적힌 **텍스트·개수·배치**는 임의로 바꾸지 않는다. | §3에 시각 속성이 없음 ↔ §9 추측 금지 |
| D2 | 반응형 브레이크포인트는 구현자가 정하되 **390px보다 커야** 한다. | §3 "화면 폭이 좁아지면"의 임계값 없음 |
| D3 | `시작` 버튼은 이번 단계에서 **클릭해도 아무 동작을 하지 않는다.** 게임 상태를 바꾸지 않는다. | §3에 버튼 동작 없음, §5가 타이머·입력 제외 |
| D4 | D1·D2로 실제 정한 값은 회차 1에서 이 섹션에 덧붙인다 (예: 셀 20px, 브레이크포인트 720px). | 이후 회차의 일관성 |
| D5 | **화면 배치를 SPEC_00 §3에서 의도적으로 이탈시킨다.** 보드를 화면 정중앙에 두고, 상태 패널을 보드 위 가로 HUD 한 줄로 옮긴다. 네온 사인 테마를 적용한다. | 사람 지시 (아래) |

**D6 — 사용자 지시 (2026-09-10, `/spec-run` 밖)**

> 블럭이 내려오는 동안 TETRIS LOOP 메인 로고가 반짝이게 바꿔주고, 줄이 높이을수록
> 반짝이는 속도가 빠르게 해주세요

`.title`은 `status === PLAYING`이고 `piece !== null`일 때만 `is-flickering` 클래스를 달고
점멸한다. 주기는 `lines`에 따라 짧아진다. D1(시각 속성 재량)의 범위를 넘는 **동작** 변경이라
사용자 지시로 기록한다. 어떤 SPEC에도 없던 요소이며, 기존에 통과한 조건을 무효화하지 않는다
(로고 관련 항목 S1은 원래부터 미확인이었다).

| 항목 | 값 |
| --- | --- |
| 점멸 조건 | `PLAYING` && `piece !== null`. `READY`·`GAME_OVER`에서는 점멸하지 않음 |
| 주기 공식 | `max(400, 2400 - lines * 200)` ms — `main.js`의 `flickerPeriodMs()` |
| 상수 | `FLICKER_BASE_MS 2400` / `FLICKER_STEP_MS 200` / `FLICKER_MIN_MS 400` |
| 전달 방식 | `main.js`가 `--flicker-period` 커스텀 속성을 `.title`에 설정 |
| **안전 상한** | 주기 하한 400ms + 사이클당 깜빡임 1회 = **최대 2.5Hz**. 광과민성 임계(3Hz) 미만 |
| 밝기 | 완전히 꺼지지 않고 `opacity: 0.35`까지만 떨어진다 |
| 접근성 | `prefers-reduced-motion: no-preference`일 때만 애니메이션이 걸린다 |

**로드 시 `power-on` 애니메이션은 제거했다.** 같은 요소에 애니메이션 두 개가 붙으면
`is-flickering`이 붙었다 떨어질 때 점등이 재실행되는 문제가 생긴다. 모션은 상태에 묶인
점멸 하나만 남긴다. `grep -c 'power-on'`이 `0`이다.

주기 하한은 사용자 지시("줄이 높을수록 빠르게")를 무한정 따르지 않고 안전선에서 끊은
것이다. 하한을 풀거나 깜빡임 횟수를 늘리려면 사용자 지시가 필요하다.

**D5 — 사용자 지시 (2026-09-10, `/spec-run` 밖)**

> 지금 테트리스 게임 디자인을 세련되고 네오사인이 번쩍번쩍하는 디자인으로 바꿔주고,
> 보드는 가운데로 바꿔주세요

이 지시는 SPEC_00 §3의 "데스크톱 화면에서 왼쪽에는 게임 보드, 오른쪽에는 상태 패널을
배치한다"와 **정면으로 충돌한다.** D1은 색·여백만 재량으로 두고 §3의 배치는 바꾸지 말라고
정해 두었으므로, 이것은 D1의 재량 범위가 아니라 **사용자가 SPEC보다 우선하여 내린 결정**이다.
`SPEC_00_PROJECT.md`는 수정하지 않았고 해석은 D5를 따른다.

D5로 바뀐 것과 지켜진 것:

| 바뀐 것 | 지켜진 것 |
| --- | --- |
| 보드가 화면 정중앙 단일 컬럼 | 페이지 제목 `TETRIS LOOP` |
| 상태 패널 → 보드 위 가로 HUD | 레이블 `점수:` `제거한 줄:` `게임 상태:` |
| 배경·색·타이포가 네온 사인 테마 | 보드 아래 `시작` 버튼 (§3) |
| 조각 7색을 네온 팔레트로 교체 | 조작 안내 문구 전체 |
| 표제에 로드 시 점등 1회 (`prefers-reduced-motion` 존중) | 10열×20행 200셀, 좁을 때 세로 배치 |

`main.js`가 쓰는 DOM id (`board`, `score-value`, `lines-value`, `status-value`,
`start-button`) 5개는 전부 유지했다. `game.js`와 `test.js`는 건드리지 않았다.

**D4로 회차 1에서 실제 정한 값** (이후 회차에서 바꾸지 않는다):

| 항목 | 값 |
| --- | --- |
| 셀 크기 / 간격 | `--cell-size: 24px` / `--cell-gap: 2px` |
| 반응형 브레이크포인트 | `max-width: 720px`에서 세로 배치 (D2 충족: 720 > 390) |
| 보드 렌더링 | `.board`를 CSS grid 10열×20행, 셀 `div.cell` 200개를 `main.js`가 생성 |
| 패널 마크업 | `dl.panel-list` + `#score-value` / `#lines-value` / `#status-value` |
| 시작 버튼 | `#start-button` — **이벤트 리스너를 붙이지 않음** (D3) |

**D1은 §9를 무력화하지 않는다.** `[사람 확인 필요]`는 여전히 **의미 있는 결정**에 발동한다 —
점수 계산식, 키 배치, 새 파일 추가, §3에 없는 화면 요소 추가, SPEC이 정의한 공개 API 변경.
순수 시각 속성만 D1의 재량 범위다.

### 추가 검증 규칙 (회차 0 · 사람 지시)

`.claude/commands/spec-run.md`는 쓰기 금지이므로, 커맨드가 5단계에서 함께 적용할 규칙을
여기에 기록한다. `/spec-run`은 1단계에서 이 섹션을 읽고 반영한다.

**R1 — 완료 조건 10의 AUTO 보조 근거.** 산출물 `*.js`에 타이머·입력 리스너가 **없음**을
확인한다. 출력이 비어 있어야 통과다.

```bash
grep -nE "setInterval|setTimeout|requestAnimationFrame|addEventListener\((\"|')key" tetris-loop-workshop/*.js
```

R1이 통과해도 완료 조건 10의 최종 판정은 `HUMAN`이다. 보조 근거로만 표에 남긴다.

**R1은 SPEC_00 전용이다.** SPEC_01은 800ms 타이머와 `keydown` 리스너를 반드시 도입하므로
R1을 적용하지 않는다. SPEC_01 회차 1에서 R1을 돌렸다면 다음 두 줄이 나왔을 것이며, 이는
설계대로다.

```text
main.js:47:    timerId = setInterval(onTick, DROP_INTERVAL_MS);
main.js:96:  document.addEventListener('keydown', onKeyDown);
```

SPEC_00 완료 조건 10("어떤 블록도 생성되거나 움직이지 않는다")은 SPEC_00 시점의 판정이며
재평가하지 않는다. 실패로 기록하지 않는다.

### SPEC_01 계약 (회차 1에서 확정)

| 대상 | 확정된 값 |
| --- | --- |
| 상태 추가 필드 | `piece` (`null` 또는 `{type, matrix, row, col}`), `randomizer` |
| 공개 추가 | `TETROMINO_TYPES`, `TETROMINOES`, `spawnPiece`, `movePiece`, `rotatePiece`, `stepDown`, `startGame`, `toRenderBoard` |
| 생성 위치 | `row 0`, `col Math.floor((10 - N) / 2)` — I는 3, O는 4, 나머지는 3 |
| 회전 | 전치 후 행 뒤집기. 보드 밖이면 취소(벽킥 없음) |
| 낙하 주기 | `DROP_INTERVAL_MS = 800` (`main.js`) |
| 조각 색 (D1 재량) | I `#23e0ff` · O `#ffd23f` · T `#ff2d95` · S `#3dff9a` · Z `#ff5a4d` · J `#5b7cff` · L `#ff9d1f` — 서로 다른 7색. **D5에서 네온 팔레트로 교체** |
| 렌더 방식 | `toRenderBoard()`가 돌려준 값으로 셀 클래스를 `cell cell-<종류>`로 바꾼다 |
| 타이머 정리 | `stopTimer()`가 `clearInterval` 후 `timerId = null`. `LANDED`와 시작 클릭에서 호출 |

### SPEC_02 계약 (회차 1에서 확정)

| 대상 | 확정된 값 |
| --- | --- |
| 보드 셀 값 | 빈 칸 `0`, 고정된 칸은 조각 종류 문자열 `'I'`~`'L'` |
| 공개 추가 | `canPlace`, `lockPiece`, `findFullRows`, `clearRows`, `resolveLanding` |
| 착지 순서 | 고정 → 완성줄 탐색 → 동시 제거 → 위쪽 압축 → `lines` 누적 → 다음 생성 (`resolveLanding`) |
| 게임오버 | 생성 자리가 막히면 `piece: null` + `status: GAME_OVER` |
| 예약 틱 가드 | `onTick()` 진입 시 `status !== PLAYING`이면 `stopTimer()` 후 즉시 반환 (`main.js:70`) |
| 생성기 연결 | `main.js`가 `globalThis.TETRIS_RANDOMIZER`가 함수면 주입 (`main.js:11`) |
| `score` | 이 SPEC 범위 밖. 항상 `0` |

### SPEC_03 계약 (회차 1에서 확정)

| 대상 | 확정된 값 |
| --- | --- |
| 공개 추가 | `LINE_SCORES`, `SOFT_DROP_POINT`, `HARD_DROP_POINT`, `lineScore`, `softDrop`, `hardDrop` |
| 줄 제거 점수 | `LINE_SCORES = [0, 100, 300, 500, 800]` (인덱스 = 한 번에 지운 줄 수) |
| 범위 밖 인덱스 | `lineScore(5+)`는 `800`, `lineScore(-1)`은 `0`. 800을 넘지 않는다 |
| 소프트 드롭 | `SOFT_DROP_POINT = 1`. 실제로 내려간 칸에만 준다 |
| 하드 드롭 | `HARD_DROP_POINT = 2`. 충돌 직전까지 내리고 **즉시** 착지 순서 수행 |
| 자동 낙하 | `stepDown()`은 점수를 주지 않는다 |
| 키 경로 분리 | `↓` → `softDrop()`, 타이머 → `stepDown()`, `Space` → `hardDrop()` |
| Space 처리 | `event.key === ' '` 또는 `'Spacebar'`. `preventDefault()`로 시작 버튼 오작동 차단 |
| 합산 | 드롭 점수 + `lineScore(n)`. 레벨 배수 없음 |
| 루프 상한 | `hardDrop()`의 탐색이 `BOARD_HEIGHT`회를 넘지 않는다 |

### SPEC_05 계약 (회차 1에서 확정)

| 대상 | 확정된 값 |
| --- | --- |
| 상수 | `LINES_PER_LEVEL 2` / `BASE_DROP_INTERVAL_MS 700` / `LEVEL_SPEED_STEP_MS 60` / `MIN_DROP_INTERVAL_MS 100` |
| 레벨 | `levelFor(lines) = floor(lines / 2) + 1` — **상태 필드가 아니라 파생값** |
| 낙하 간격 | `dropIntervalFor(level) = max(100, 700 - (level - 1) * 60)` |
| 점수 배수 | 줄 점수에만 곱한다. 배수는 **줄을 지우기 전** `lines`로 계산한 레벨 |
| 드롭 점수 | 소프트 1점 / 하드 2점, **배수 없음** |
| 타이머 | `main.js`의 `syncTimer()` — 목표 간격이 **실제로 달라졌을 때만** 다시 건다 |
| HUD | `점수: / 제거한 줄: / 레벨: / 게임 상태:` 순. `#level-value` |

값 표: 레벨 1~10 → `700, 640, 580, 520, 460, 400, 340, 280, 220, 160` ms,
레벨 11 이상 → `100` ms 고정.

**D7 — HUD에 `레벨:` 추가 (사용자 지시, 2026-09-10).** SPEC_00 §3에 없던 화면 요소를
더한 것이라 D5와 같은 성격의 결정으로 기록한다. SPEC_00 §3 세 항목의 상대 순서
(점수 → 제거한 줄 → 게임 상태)는 유지했다.

### SPEC_01에서 무효가 된 항목 (SPEC_05 §9 · 회차 1에서 기록)

SPEC_05가 **대체**한 것이며 실패가 아니다.

1. SPEC_01 계약 `DROP_INTERVAL_MS = 800` — 이제 고정 상수가 아니라 레벨에서 계산된다.
   레벨 1의 간격은 `700ms`다. `main.js`에서 `DROP_INTERVAL_MS`와 `800` 리터럴이 모두
   사라졌다 (grep 0건).
2. SPEC_01 완료 조건 19 (HUMAN, 미확인이었음) — "약 800ms마다 한 칸씩 내려간다".
   레벨 1에서 `700ms`가 되므로 대체한다. **확인 요청 목록에서 뺀다.**

SPEC_03 §5와 SPEC_04 §5의 범위 밖 항목 "레벨과 낙하 속도 변화", "레벨에 따른 점수 배수"를
SPEC_05가 구현했다. 후속 SPEC이 범위를 넓히는 정상적인 진행이다.

### SPEC_04 계약 (회차 1에서 확정)

| 대상 | 확정된 값 |
| --- | --- |
| 저장 키 | `STORAGE_KEY = 'tetris-loop.leaderboard.v1'` — 다른 키는 쓰지 않는다 |
| 상수 | `NAME_MIN_LENGTH 2` / `NAME_MAX_LENGTH 10` / `LEADERBOARD_LIMIT 10` |
| 허용 문자 | `/^[가-힣ㄱ-ㅎㅏ-ㅣA-Za-z0-9]+$/` — 공백 불허 |
| 검증 순서 | trim → 빈 값 → 최소 길이 → 최대 길이 → 문자 종류 |
| 기록 필드 | `id`(문자열) · `name` · `score` · `clearedLines` · `playedAt`(ISO 8601) |
| 정렬 | `score` 내림차순 → `name` 오름차순(코드 단위) → `playedAt` 오름차순 → 기존 순서(안정 정렬) |
| 손상 복구 | 문자열 아님·파싱 실패·배열 아님·필드 누락·타입 불일치 → 전부 빈 배열 |
| 저장소 주입 | `loadLeaderboard(storage)` 등이 저장소를 **인자로** 받는다. `game.js`는 전역 `localStorage`를 참조하지 않는다 (grep 0건) |
| 예외 흡수 | 읽기 실패 → `[]`, 쓰기·초기화 실패 → `false` |
| 한 게임 1회 저장 | `main.js`의 지역 변수 `savedThisGame`. 게임 상태 객체에 넣지 않는다 |
| 초기화 확인 문구 | `저장된 기록을 모두 지울까요?` (`main.js`의 `CLEAR_CONFIRM_TEXT`) |
| 새 DOM id | `gameover-panel`, `final-score`, `final-lines`, `save-form`, `name-input`, `save-button`, `save-message`, `leaderboard-list`, `leaderboard-empty`, `clear-button` |
| `maxlength` | **두지 않는다.** 두면 11자 입력이 막혀 길이 초과 메시지를 확인할 수 없다 |
| `hidden` 보호 | `.gameover[hidden] { display: none; }`로 `display: flex`가 `hidden`을 무력화하지 않게 한다 |

**SPEC_00 §5의 "리더보드와 브라우저 저장"을 SPEC_04가 구현했다.** 후속 SPEC이 범위를
넓히는 정상적인 진행이며 실패로 기록하지 않는다.

**미해결 위험 — `file://`의 `localStorage`.** 브라우저에 따라 저장소 접근이 예외를
던지거나 페이지마다 격리될 수 있다. 이 경우 코드가 옳아도 "새로 고쳐도 기록이 남는다"가
실패한다. 실패하면 **코드를 고치기 전에 다른 브라우저에서 먼저 확인**하고, 환경 문제로
판명되면 여기에 환경 제약으로 기록한 뒤 사용자에게 보고한다. 같은 수정을 두 번 시도하지
않는다 (SPEC_04 §9).

### SPEC_02에서 무효가 된 항목 (SPEC_03 §9 · 회차 1에서 기록)

SPEC_03이 **대체**한 것이며 실패가 아니다.

1. SPEC_02 완료 조건 23 (AUTO, 회차 1에 통과했던 것) — "`score`가 어떤 경우에도 `0`을
   유지한다". SPEC_03이 점수를 도입했으므로 더 이상 성립하지 않는다.
2. SPEC_01 §5·SPEC_02 §5의 범위 밖 항목 "점수 계산", "하드 드롭(Space)" — SPEC_03이
   구현했다. 후속 SPEC이 범위를 넓히는 정상적인 진행이다.

### SPEC_01에서 무효가 된 항목 (SPEC_02 §9 · 회차 1에서 기록)

셋 다 SPEC_02가 **대체**한 것이며 실패가 아니다.

1. SPEC_01 완료 조건 11 (AUTO, 회차 1에 통과했던 것) — "바닥에서 `stepDown()`이
   `status`를 `LANDED`로 바꾼다". 이제 착지 순서를 수행하고 `PLAYING`을 유지한다.
2. SPEC_01 완료 조건 23 (HUMAN, 미확인이었음) — "바닥에 닿으면 `LANDED`가 되고 멈춘다".
   이제 곧바로 다음 조각이 나온다.
3. SPEC_01 §4-5 — "`LANDED` 이후 새 조각을 생성하지 않는다".

`GAME_STATUS.LANDED`는 SPEC_00 §4의 다섯 상태 계약 때문에 상수로 남지만 **어떤 함수도
반환하지 않는다**. `grep -c 'GAME_STATUS\.LANDED'`가 `game.js`·`main.js` 모두 `0`이다.

SPEC_01 완료 조건 8·10(경계 밖 이동·회전 무시)은 유효하며 고정 블록 겹침 검사가 더해졌다.

---

## 완료 조건 현황

### SPEC_05 §6 (활성) — 21개 · AUTO 14 / HUMAN 7

| # | 완료 조건 | 수단 | 상태 | 근거 |
| - | --- | --- | --- | --- |
| 1 | 상수가 `2`, `700`, `60`, `100` | AUTO | **통과** | 회차 1 · 5-5f C1 |
| 2 | `levelFor(0)`과 `levelFor(1)`이 `1` | AUTO | **통과** | 회차 1 · 5-5f C2 |
| 3 | `levelFor()`가 두 줄마다 `1`씩 오름 | AUTO | **통과** | 회차 1 · 5-5f C3 (0~11줄) |
| 4 | `dropIntervalFor(1)`이 `700` | AUTO | **통과** | 회차 1 · 5-5f C4 |
| 5 | 레벨당 `60`씩 감소 | AUTO | **통과** | 회차 1 · 5-5f C5 (레벨 1~10) |
| 6 | `100` 아래로 내려가지 않음 | AUTO | **통과** | 회차 1 · 5-5f C6 (레벨 11~999) |
| 7 | 줄 점수에 지우기 **전** 레벨이 곱해짐 | AUTO | **통과** | 회차 1 · 5-5f C7 (300, 600 아님) |
| 8 | 레벨 `1`에서 한 줄 = `100`점 | AUTO | **통과** | 회차 1 · 5-5f C8 |
| 9 | 레벨 `2`에서 한 줄 = `200`점 | AUTO | **통과** | 회차 1 · 5-5f C9 |
| 10 | 레벨 `2`에서 네 줄 = `1600`점 | AUTO | **통과** | 회차 1 · 5-5f C10 |
| 11 | 소프트 드롭에 배수 없음 | AUTO | **통과** | 회차 1 · 5-5f C11 (레벨 6에서 5점) |
| 12 | 하드 드롭에 배수 없음 | AUTO | **통과** | 회차 1 · 5-5f C12 (레벨 6에서 36점) |
| 13 | 상태 필드가 여전히 여섯 개 | AUTO | **통과** | 회차 1 · 5-5f C13·X1 |
| 14 | `startGame()` 후 레벨이 `1` | AUTO | **통과** | 회차 1 · 5-5f C14 |
| 15 | `test.html` 모든 테스트 통과 | HUMAN | 미확인 | - |
| 16 | `index.html` 콘솔 오류 없음 | HUMAN | 미확인 | - |
| 17 | HUD에 `레벨:` 항목과 값이 보인다 | HUMAN | 미확인 | 보조: `#level-value` 존재 |
| 18 | 시작 직후 `레벨:`이 `1` | HUMAN | 미확인 | 보조: 5-5f C14 |
| 19 | 두 줄을 지우면 `레벨:`이 `2` | HUMAN | 미확인 | 보조: 5-5f C3 |
| 20 | 레벨이 오르면 눈에 띄게 빨라진다 | HUMAN | 미확인 | 보조: 5-5f X2 (700→580ms) |
| 21 | 방향키 연타해도 자동 낙하가 멈추지 않는다 | HUMAN | 미확인 | 보조: `syncTimer()` 가드 |

**AUTO 14건 통과, HUMAN 7건 미확인. 완료 아님.**

조건 20·21은 **시간 경과를 눈으로 봐야만** 판정된다. 공식과 가드가 옳은 것은 AUTO로
확인했지만 타이머가 실제로 그 간격으로 도는지는 코드로 알 수 없다.

### SPEC_04 §6 — 33개 · AUTO 22 / HUMAN 11

| # | 완료 조건 | 수단 | 상태 | 근거 |
| - | --- | --- | --- | --- |
| 1 | `STORAGE_KEY`가 `tetris-loop.leaderboard.v1` | AUTO | **통과** | 회차 1 · 5-5e C1 |
| 2 | `validateName()`이 앞뒤 공백 제거 | AUTO | **통과** | 회차 1 · 5-5e C2 |
| 3 | 빈 이름 거부 + 이유 | AUTO | **통과** | 회차 1 · 5-5e C3 |
| 4 | 1자 이름 거부 + 이유 | AUTO | **통과** | 회차 1 · 5-5e C4 |
| 5 | 11자 이름 거부 + 이유 | AUTO | **통과** | 회차 1 · 5-5e C5 |
| 6 | 허용되지 않는 문자 거부 + 이유 | AUTO | **통과** | 회차 1 · 5-5e C6 (이모지 포함 5종) |
| 7 | 한글 완성형·자모·영문·숫자 통과 | AUTO | **통과** | 회차 1 · 5-5e C7 (6종) |
| 8 | `createEntry()`가 다섯 필드를 만든다 | AUTO | **통과** | 회차 1 · 5-5e C8 |
| 9 | 점수 내림차순 정렬 | AUTO | **통과** | 회차 1 · 5-5e C9 |
| 10 | 동점이면 이름 오름차순 | AUTO | **통과** | 회차 1 · 5-5e C10 |
| 11 | 이름까지 같으면 `playedAt` 오름차순 | AUTO | **통과** | 회차 1 · 5-5e C11 |
| 12 | 셋 다 같으면 기존 순서 유지 | AUTO | **통과** | 회차 1 · 5-5e C12 |
| 13 | `sortEntries()`가 원본을 바꾸지 않는다 | AUTO | **통과** | 회차 1 · 5-5e C13 |
| 14 | `addEntry()`가 열 개 이하 유지 | AUTO | **통과** | 회차 1 · 5-5e C14 |
| 15 | 점수 낮은 11번째는 밀려난다 | AUTO | **통과** | 회차 1 · 5-5e C15 |
| 16 | JSON이 아닌 문자열 → 빈 배열 | AUTO | **통과** | 회차 1 · 5-5e C16 |
| 17 | 배열이 아닌 JSON → 빈 배열 | AUTO | **통과** | 회차 1 · 5-5e C17·C17b |
| 18 | 필드 누락·타입 불일치 → 빈 배열 | AUTO | **통과** | 회차 1 · 5-5e C18 |
| 19 | `loadLeaderboard()`가 예외를 빈 배열로 | AUTO | **통과** | 회차 1 · 5-5e C19·C19b |
| 20 | `saveLeaderboard()`가 예외에 `false` | AUTO | **통과** | 회차 1 · 5-5e C20 |
| 21 | `saveLeaderboard()`가 `STORAGE_KEY`에 쓴다 | AUTO | **통과** | 회차 1 · 5-5e C21·C21b·C21c |
| 22 | `clearLeaderboard()`가 키를 지운다 | AUTO | **통과** | 회차 1 · 5-5e C22·C22b |
| 23 | `test.html` 모든 테스트 통과 | HUMAN | 미확인 | - |
| 24 | `index.html` 콘솔 오류 없음 | HUMAN | 미확인 | - |
| 25 | 게임오버 전에는 패널이 화면에 없다 | HUMAN | 미확인 | 보조: `hidden` 속성 + `.gameover[hidden]` |
| 26 | 게임오버 시 최종 점수·줄 수 표시 | HUMAN | 미확인 | - |
| 27 | 잘못된 이름은 이유 표시 + 저장 안 됨 | HUMAN | 미확인 | 보조: 5-5e C3~C6 |
| 28 | 유효한 이름 저장 시 목록에 나타남 | HUMAN | 미확인 | 보조: 5-5e X2 |
| 29 | 같은 게임 두 번째 저장 거부 | HUMAN | 미확인 | - |
| 30 | 새로 고쳐도 기록이 남는다 | HUMAN | 미확인 | **`file://` 저장소 위험 있음** |
| 31 | 초기화 취소 시 목록 그대로 | HUMAN | 미확인 | `confirm()`은 자동 검증 불가 |
| 32 | 초기화 확인 시 목록이 빈다 | HUMAN | 미확인 | `confirm()`은 자동 검증 불가 |
| 33 | 새 게임 시작하면 패널이 사라진다 | HUMAN | 미확인 | - |

**AUTO 22건 통과, HUMAN 11건 미확인. 완료 아님.**

### SPEC_03 §6 — 27개 · AUTO 20 / HUMAN 7

| # | 완료 조건 | 수단 | 상태 | 근거 |
| - | --- | --- | --- | --- |
| 1 | `LINE_SCORES`가 `[0,100,300,500,800]` | AUTO | **통과** | 회차 1 · 5-5d C1 |
| 2 | `lineScore()`가 0~4줄에 0/100/300/500/800 | AUTO | **통과** | 회차 1 · 5-5d C2 |
| 3 | 한 줄 제거 시 `score` +100 | AUTO | **통과** | 회차 1 · 5-5d C3 |
| 4 | 두 줄 동시 제거 시 `score` +300 | AUTO | **통과** | 회차 1 · 5-5d C4 |
| 5 | 네 줄 동시 제거 시 `score` +800 | AUTO | **통과** | 회차 1 · 5-5d C5 |
| 6 | `softDrop()`이 한 칸 내린다 | AUTO | **통과** | 회차 1 · 5-5d C6 |
| 7 | `softDrop()` 한 칸에 `score` +1 | AUTO | **통과** | 회차 1 · 5-5d C7·C7b |
| 8 | 못 내려가면 `softDrop()` 점수 없음 | AUTO | **통과** | 회차 1 · 5-5d C8 |
| 9 | 못 내려가면 `softDrop()`이 착지 처리 | AUTO | **통과** | 회차 1 · 5-5d C9 |
| 10 | 자동 낙하 `stepDown()`은 점수 없음 | AUTO | **통과** | 회차 1 · 5-5d C10 |
| 11 | `hardDrop()`이 더 못 가는 행까지 내린다 | AUTO | **통과** | 회차 1 · 5-5d C11·C15 |
| 12 | `hardDrop()` 점수 = 칸수 × 2 | AUTO | **통과** | 회차 1 · 5-5d C12 (18칸→36) |
| 13 | `hardDrop()` 후 보드에 고정 | AUTO | **통과** | 회차 1 · 5-5d C13 |
| 14 | `hardDrop()` 후 새 조각 생성 | AUTO | **통과** | 회차 1 · 5-5d C14 |
| 15 | 0칸 하드 드롭은 점수 없음 | AUTO | **통과** | 회차 1 · 5-5d C16 |
| 16 | 드롭 점수와 줄 점수가 더해진다 | AUTO | **통과** | 회차 1 · 5-5d C17 (36+100=136) |
| 17 | `startGame()`이 `score`를 0으로 | AUTO | **통과** | 회차 1 · 5-5d C18 |
| 18 | `startGame()`이 `lines`를 0으로 | AUTO | **통과** | 회차 1 · 5-5d C19 |
| 19 | `GAME_OVER`에서 `hardDrop()` 무변 | AUTO | **통과** | 회차 1 · 5-5d C20 |
| 20 | `GAME_OVER`에서 `softDrop()` 무변 | AUTO | **통과** | 회차 1 · 5-5d C21 |
| 21 | `test.html` 모든 테스트 통과 | HUMAN | 미확인 | - |
| 22 | `index.html` 콘솔 오류 없음 | HUMAN | 미확인 | - |
| 23 | 줄을 지우면 `점수:` 표시가 즉시 오른다 | HUMAN | 미확인 | 보조: 5-5d C3 |
| 24 | `↓`를 누를 때마다 `점수:`가 1씩 오른다 | HUMAN | 미확인 | 보조: 5-5d C7b |
| 25 | `Space`로 조각이 끝까지 내려간다 | HUMAN | 미확인 | 보조: 5-5d C11 |
| 26 | `Space`를 눌러도 재시작되지 않는다 | HUMAN | 미확인 | 보조: `preventDefault` (`main.js:127`) |
| 27 | 시작 버튼이 `점수:`와 `제거한 줄:`을 0으로 | HUMAN | 미확인 | 보조: 5-5d C18·C19 |

**AUTO 20건 통과, HUMAN 7건 미확인. 완료 아님.**

### SPEC_02 §6 — 32개 · AUTO 23 / HUMAN 9 (조건 23은 SPEC_03이 대체)

| # | 완료 조건 | 수단 | 상태 | 근거 |
| - | --- | --- | --- | --- |
| 1 | `canPlace()`가 고정 블록과 겹치면 `false` | AUTO | **통과** | 회차 1 · 5-5c C1 |
| 2 | `canPlace()`가 빈 보드 유효 위치에 `true` | AUTO | **통과** | 회차 1 · 5-5c C2 |
| 3 | 고정 블록과 겹치는 `movePiece()` 무시 | AUTO | **통과** | 회차 1 · 5-5c C3 |
| 4 | 고정 블록과 겹치는 `rotatePiece()` 무시 | AUTO | **통과** | 회차 1 · 5-5c C4 |
| 5 | `lockPiece()`가 종류 문자열을 쓴다 | AUTO | **통과** | 회차 1 · 5-5c C5 |
| 6 | `lockPiece()` 후 `piece`가 `null` | AUTO | **통과** | 회차 1 · 5-5c C6 |
| 7 | `lockPiece()`가 원본 보드를 바꾸지 않는다 | AUTO | **통과** | 회차 1 · 5-5c C7 |
| 8 | `findFullRows()`가 가득 찬 행을 오름차순으로 | AUTO | **통과** | 회차 1 · 5-5c C8 |
| 9 | `findFullRows()`가 없으면 빈 배열 | AUTO | **통과** | 회차 1 · 5-5c C9 |
| 10 | `clearRows()`가 지정 행을 없앤다 | AUTO | **통과** | 회차 1 · 5-5c C10 |
| 11 | `clearRows()` 후 위쪽 행이 내려온다 | AUTO | **통과** | 회차 1 · 5-5c C11 |
| 12 | `clearRows()` 후 20행 10열 유지 | AUTO | **통과** | 회차 1 · 5-5c C12 |
| 13 | 두 줄 동시 완성 시 한 번에 제거 | AUTO | **통과** | 회차 1 · 5-5c C14 |
| 14 | 제거한 줄 수가 `lines`에 누적 | AUTO | **통과** | 회차 1 · 5-5c C15 |
| 15 | 착지 후 새 조각이 `row 0`에 생성 | AUTO | **통과** | 회차 1 · 5-5c C16 |
| 16 | 착지 후 `status`가 `PLAYING` | AUTO | **통과** | 회차 1 · 5-5c C17 |
| 17 | 어떤 함수도 `LANDED`를 반환하지 않는다 | AUTO | **통과** | 회차 1 · 5-5c C18, grep 0건 |
| 18 | 생성 위치가 막히면 `GAME_OVER` | AUTO | **통과** | 회차 1 · 5-5c C20 |
| 19 | `GAME_OVER`면 `piece`가 `null` | AUTO | **통과** | 회차 1 · 5-5c C21 |
| 20 | `GAME_OVER`에서 `stepDown()` 무변 | AUTO | **통과** | 회차 1 · 5-5c C22 |
| 21 | `GAME_OVER`에서 `movePiece()` 무변 | AUTO | **통과** | 회차 1 · 5-5c C23 |
| 22 | `startGame()`이 `GAME_OVER`에서 빈 보드로 | AUTO | **통과** | 회차 1 · 5-5c C24 |
| 23 | `score`가 항상 `0` | AUTO | **대체됨** | SPEC_03이 점수를 도입 (실패 아님) |
| 24 | `test.html` 모든 테스트 통과 | HUMAN | 미확인 | - |
| 25 | `index.html` 콘솔 오류 없음 | HUMAN | 미확인 | - |
| 26 | 조각이 쌓인 블록 위에 멈추고 새 조각이 나온다 | HUMAN | 미확인 | 보조: 5-5c C19 |
| 27 | 쌓인 블록을 방향키로 뚫을 수 없다 | HUMAN | 미확인 | 보조: 5-5c C3 |
| 28 | 가득 찬 줄이 사라지고 위가 내려온다 | HUMAN | 미확인 | 보조: 5-5c C11·C15 |
| 29 | `제거한 줄:` 표시가 올라간다 | HUMAN | 미확인 | - |
| 30 | 위까지 쌓이면 `GAME_OVER` 표시 후 정지 | HUMAN | 미확인 | 보조: 5-5c X1·X2 |
| 31 | `GAME_OVER`에서 방향키 무반응 | HUMAN | 미확인 | 보조: 5-5c C23 |
| 32 | `GAME_OVER`에서 시작 버튼이 재시작 | HUMAN | 미확인 | 보조: 5-5c C24 |

**AUTO 23건 통과, HUMAN 9건 미확인. 완료 아님.**

### D6 확인 항목 (완료 조건 아님 · 사람 지시로 추가)

| # | 항목 | 수단 | 상태 | 근거 |
| - | --- | --- | --- | --- |
| F1 | 조각이 내려오는 동안 로고가 점멸한다 | HUMAN | 미확인 | - |
| F2 | `READY`와 `GAME_OVER`에서는 점멸하지 않는다 | HUMAN | 미확인 | - |
| F3 | 줄을 지울수록 점멸이 빨라진다 | HUMAN | 미확인 | 보조: 주기 곡선 계산 |
| F4 | 눈이 피로할 만큼 과하지 않다 | HUMAN | 미확인 | 보조: 최대 2.5Hz 계산 |

### SPEC_01 §6 — 24개 · AUTO 13 / HUMAN 11 (조건 11·23은 SPEC_02가 대체)

| # | 완료 조건 | 수단 | 상태 | 근거 |
| - | --- | --- | --- | --- |
| 1 | `TETROMINO_TYPES`가 일곱 종류다 | AUTO | **통과** | 회차 1 · 5-5b C1 |
| 2 | 일곱 기준 행렬이 §4-3과 일치한다 | AUTO | **통과** | 회차 1 · 5-5b C2 |
| 3 | `createInitialState()`가 기존 4필드를 유지한다 | AUTO | **통과** | 회차 1 · 5-5 `KEYS`, 5-5b C3 |
| 4 | 주입한 생성기가 `spawnPiece()`에서 사용된다 | AUTO | **통과** | 회차 1 · 5-5b C4 |
| 5 | 기본 생성기가 `TETROMINO_TYPES` 값을 반환한다 | AUTO | **통과** | 회차 1 · 5-5b C5 (200회) |
| 6 | 생성 위치가 `row 0`, `col floor((10-N)/2)`다 | AUTO | **통과** | 회차 1 · 5-5b C6 (7종 전부) |
| 7 | 경계 안에서 `movePiece()`가 `dx`,`dy`만큼 옮긴다 | AUTO | **통과** | 회차 1 · 5-5b C7 |
| 8 | 경계 밖 `movePiece()`는 위치를 바꾸지 않는다 | AUTO | **통과** | 회차 1 · 5-5b C8 |
| 9 | `rotatePiece()`가 전치 후 행 뒤집기와 같다 | AUTO | **통과** | 회차 1 · 5-5b C9 (7종 + 4회전 복귀) |
| 10 | 경계 밖 회전은 행렬·위치를 바꾸지 않는다 | AUTO | **통과** | 회차 1 · 5-5b C10 |
| 11 | 바닥에서 `stepDown()`이 `LANDED`로 바꾼다 | AUTO | **대체됨** | SPEC_02가 착지 순서로 대체 (실패 아님) |
| 12 | `toRenderBoard()`가 조각 칸에 종류를 채운다 | AUTO | **통과** | 회차 1 · 5-5b C12 |
| 13 | `startGame()`이 보드·점수·줄을 초기화한다 | AUTO | **통과** | 회차 1 · 5-5b C13 |
| 14 | `test.html` 모든 테스트 통과 | HUMAN | 미확인 | - |
| 15 | `index.html` 콘솔 오류 없음 | HUMAN | 미확인 | - |
| 16 | 시작 버튼을 누르면 `PLAYING`으로 표시된다 | HUMAN | 미확인 | - |
| 17 | 시작 버튼을 누르면 조각이 위쪽 가운데에 나타난다 | HUMAN | 미확인 | - |
| 18 | 일곱 조각이 서로 구별되는 색으로 보인다 | HUMAN | 미확인 | 보조: 고유 색 7개 확인됨 |
| 19 | 약 800ms마다 한 칸씩 내려간다 | HUMAN | **대체됨** | SPEC_05가 레벨 1 = 700ms로 대체 |
| 20 | `←` `→` `↓`로 한 칸씩 움직인다 | HUMAN | 미확인 | - |
| 21 | `↑`로 시계방향 회전한다 | HUMAN | 미확인 | - |
| 22 | 좌우 벽 끝에서 더 이동하지 않는다 | HUMAN | 미확인 | - |
| 23 | 바닥에 닿으면 `LANDED`가 되고 멈춘다 | HUMAN | **대체됨** | SPEC_02가 대체. 확인 요청하지 않는다 |
| 24 | `LANDED`에서 시작 버튼을 누르면 새 조각이 나온다 | HUMAN | 미확인 | - |

**AUTO 13건 통과, HUMAN 11건 미확인. 완료 아님.**

### SPEC_00 §6 (완료) — 10개 전부 통과

상태는 `통과(AUTO)` / `통과(HUMAN)` / `실패` / `미확인` 중 하나다.

| # | 완료 조건 | 수단 | 상태 | 근거 |
| - | --- | --- | --- | --- |
| 1 | 여섯 개 파일만으로 실행·검증할 수 있다 | AUTO | **통과** | 회차 1 · 5-1 정확히 6개 파일 / 5-3 외부 참조 0건 |
| 2 | `index.html`을 브라우저에서 직접 열었을 때 콘솔 오류가 없다 | HUMAN | **재확인 필요** | 회차 2 근거는 D5 이전 화면에 대한 것 |
| 3 | `test.html`을 브라우저에서 직접 열었을 때 모든 테스트가 통과한다 | HUMAN | **통과** | 회차 2 · 사람 확인 기록 [2026-09-10] |
| 4 | `index.html`이 `style.css`, `game.js`, `main.js`를 올바른 순서로 직접 불러온다 | AUTO | **통과** | 회차 1 · 5-2 줄 7→8→9, `defer` 2개, `test.js` 미참조 |
| 5 | 패키지 설치, 개발 서버, 빌드 명령이 필요하지 않다 | AUTO | **통과** | 회차 1 · 5-1 설정·의존 파일 없음 / 5-3 `package.json`·`node_modules` 0건 |
| 6 | 게임 보드가 정확히 10열×20행으로 표시된다 | HUMAN | **재확인 필요** | 회차 2 근거는 D5 이전 화면에 대한 것 |
| 7 | 점수 `0`, 제거한 줄 `0`, 상태 `READY`가 표시된다 | HUMAN | **재확인 필요** | 회차 2 근거는 D5 이전 화면에 대한 것 |
| 8 | 시작 버튼과 전체 조작 안내가 표시된다 | HUMAN | **재확인 필요** | 회차 2 근거는 D5 이전 화면에 대한 것 |
| 9 | 브라우저 폭 390px에서 가로 스크롤 없이 확인할 수 있다 | HUMAN | **재확인 필요** | 회차 2 근거는 D5 이전 화면에 대한 것. 보조 계산: 390px에서 페이지 폭 290px |
| 10 | 어떤 블록도 생성되거나 움직이지 않는다 | HUMAN | **통과** | 회차 2 · 사람 확인 기록 [2026-09-10] (R1 보조 근거도 통과) |

**D5 디자인 변경으로 조건 2, 6, 7, 8, 9의 HUMAN 근거가 무효가 되었다.**
회차 2의 사람 확인은 지금은 존재하지 않는 화면에 대한 것이었다. 조건 1, 4, 5는 AUTO로
재검증되었고, 조건 3(`test.html`)과 10은 `game.js`·`test.js`를 건드리지 않아 유지한다.
조건 2(콘솔 오류)는 `index.html` 구조가 바뀌었으므로 재확인 대상이다.

### 보조 자동 검증 (SPEC §7 — 완료 조건은 아니지만 AUTO 근거로 쓴다)

| 항목 | 상태 | 근거 |
| --- | --- | --- |
| `TetrisGame`과 필수 함수 존재 | **통과** | 회차 1 · 5-5 |
| `BOARD_WIDTH`/`BOARD_HEIGHT` 상수 | **통과** | 회차 1 · 5-5 |
| 빈 보드 20×10, 모든 셀 `0` | **통과** | 회차 1 · 5-5 |
| 두 보드가 행 배열을 공유하지 않음 | **통과** | 회차 1 · 5-5 |
| 초기 상태의 점수·줄·게임 상태 | **통과** | 회차 1 · 5-5 `SUMMARY pass=14 fail=0` |
| R1 — 타이머·키 리스너 부재 | **통과** | 회차 1 · R1 grep 출력 0줄 |
| 문법 검사 3개 파일 | **통과** | 회차 1 · 5-4 `node --check` 전부 OK |

이 표가 전부 통과해도 완료 조건 3(`test.html` 화면 결과)은 여전히 `HUMAN`이다.

### SPEC §6에 없는 확인 항목 (회차 0 · 사람 지시로 추가)

SPEC §3 화면 정의 중 §6 완료 조건이 덮지 못하는 항목이다. 완료 조건은 아니지만
회차 1의 사람 확인 요청 목록에 반드시 포함한다.

| # | 항목 | 수단 | 상태 | 근거 |
| - | --- | --- | --- | --- |
| S1 | 페이지 제목 `TETRIS LOOP`가 표시된다 | HUMAN | 미확인 | - |
| S2 | 데스크톱 폭에서 보드가 왼쪽, 상태 패널이 오른쪽에 배치된다 | HUMAN | 미확인 | - |
| S3 | 390px에서 보드와 상태 패널이 세로로 배치된다 | HUMAN | 미확인 | - |
| S4 | `시작` 버튼을 눌러도 화면이 변하지 않는다 (D3) | HUMAN | 미확인 | - |

회차 2에서 사람이 전달한 것은 **완료 조건 7건**이며 S1~S4는 언급되지 않았다.
S1~S4는 완료 조건이 아니므로 SPEC_00 완료를 막지 않지만, 확인되지 않은 채 남아 있다.

S3은 완료 조건 9(가로 스크롤 없음)와 별개다. 스크롤이 없어도 세로 배치 전환이 안 될 수 있다.

---

## 사람 확인 기록

`[2026-09-10] (사람) spec_00 사람이 확인할 7가지는 모두 끝났습니다`

이 한 줄이 완료 조건 2, 3, 6, 7, 8, 9, 10 — HUMAN 항목 7건 전부에 대한 통과 근거다.
사람이 항목별로 나누어 말하지 않았으므로 위 문장이 원문이며, 에이전트가 항목에 배분한
것은 HUMAN 조건이 정확히 이 7개뿐이기 때문이다. S1~S4는 이 문장에 포함되지 않는다.

`[2026-09-10] (사람) 사람이 직접확인 하겠습니다` — 브라우저 확인은 앞으로도 사람이 맡는다.
브라우저 확장 도구는 사용하지 않는다. `CLAUDE.md` §4의 HUMAN 전용 규칙은 그대로 유지한다.

> 형식: `[YYYY-MM-DD] (사람) index.html 콘솔 오류 없음 / test.html 12 통과 0 실패`

---

## 반복된 실패

| 시그니처 | 횟수 | 시도한 수정 | 결과 |
| --- | --- | --- | --- |
| (없음) | - | - | - |

> 시그니처 = `완료 조건 번호 + 증상 한 줄`. 횟수가 2가 되면 `CLAUDE.md` §3 (b) 정지 조건이 발동한다.

---

## 열린 질문 [사람 확인 필요]

(없음)

---

## 다음 실행이 할 일

**SPEC_05 회차 1은 코드 작업을 마쳤다. AUTO 14건 전부 통과. 남은 것은 전부 사람의
브라우저 확인이다.**

**확인 백로그가 55건이다.** 여섯 개 SPEC이 쌓이는 동안 브라우저 확인은 SPEC_00 정적 화면
한 번뿐이었다. AUTO가 보증하는 것은 `game.js`의 순수 로직뿐이고, 키 입력·타이머·렌더링·
CSS·폼·저장소는 실물로 확인된 적이 없다. **다음 SPEC을 얹기 전에 이것부터 처리해야 한다.**

| 묶음 | 건수 |
| --- | --- |
| SPEC_05 HUMAN | 7 |
| SPEC_04 HUMAN | 11 |
| SPEC_03 HUMAN | 7 |
| SPEC_02 HUMAN | 9 |
| SPEC_01 HUMAN (조건 19·23 제외) | 9 |
| SPEC_00 D5 재확인 (조건 2·6·7·8·9) | 5 |
| S1~S3 | 3 |
| F1~F4 (D6 로고 점멸) | 4 |

**우선 확인 4건** — 여기서 막히면 나머지가 무의미하다.
1. `index.html` 콘솔 오류 (전체의 전제)
2. `test.html` 통과 개수 (132개 예상)
3. 새로 고쳐도 기록이 남는지 (`file://` 저장소 위험)
4. 방향키를 연타해도 자동 낙하가 멈추지 않는지 (`syncTimer()` 가드)

`globalThis.TETRIS_RANDOMIZER`를 콘솔에서 고정한 뒤 확인하면 줄 제거와 게임오버를
빠르게 재현할 수 있다 (SPEC_02 §8).

--- 이전 회차 지침 ---

1. 사람이 SPEC_01 완료 조건 14~24와 S1~S4를 브라우저에서 확인하고 결과를 프롬프트로
   전달한다. 이것이 오면 회차 2를 실행한다.
2. 회차 2는 **사람이 실패로 보고한 항목만** 고친다. AUTO 13건을 만족시키는 코드는
   건드리지 않는다. 특히 `game.js`의 순수 함수는 전부 검증되었다.
3. 확인 결과가 전부 통과면 회차 2는 코드를 고치지 않고 `사람 확인 기록`만 채운 뒤
   완료를 선언한다.
4. `확정된 계약`의 D1~D4, 이름 계약, `SPEC_01 계약` 표를 바꾸지 않는다. 특히 생성 위치
   공식, 회전 규칙, 조각 7색, `DROP_INTERVAL_MS = 800`은 고정이다.
5. **R1은 계속 적용하지 않는다.** `setInterval`과 `keydown`은 SPEC_01의 요구사항이다.
6. **회차 예산 경고**: 회차 3을 마친 시점에 `미확인`/`실패`가 5건 이상이면 SPEC 분할을
   제안하고 정지한다 (SPEC_01 §9).
7. **D5 디자인 변경분 재확인.** SPEC_00 조건 2, 6, 7, 8, 9와 S1~S3을 SPEC_01 확인 요청에
   함께 넣는다. 이전 근거는 D5 이전 화면에 대한 것이라 무효다.
8. D5로 정한 네온 팔레트와 중앙 배치는 이후 회차에서 바꾸지 않는다. 되돌리려면 사용자
   지시가 필요하다.
