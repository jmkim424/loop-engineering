---
id: SPEC_02
title: 충돌·고정·줄 제거·게임오버
depends_on: [SPEC_00, SPEC_01]
max_iterations: 5
---

# SPEC 02 — 충돌·고정·줄 제거·게임오버

> **선행 조건 주의 — 확인 백로그가 쌓여 있다**
> `SPEC_01`의 HUMAN 11건이 아직 `미확인`이고, `SPEC_00`은 D5 디자인 변경으로 조건
> 2·6·7·8·9가 `재확인 필요`, S1~S3도 미확인이다. 이 SPEC의 실행을 시작하기 전에 밀린
> 브라우저 확인을 끝내는 것을 권장한다. 끝내지 않고 진행하면 실패가 났을 때 원인이 어느
> SPEC인지 가려내기 어려워진다.

## 1. 목표

움직이는 조각과 고정된 블록을 분리해 관리하고, 고정 블록과의 충돌을 판정하고, 조각이 더
내려갈 수 없으면 보드에 고정한 뒤 완성된 줄을 한꺼번에 지우고 위쪽을 압축하며, 새 조각의
생성 위치가 막히면 게임오버로 멈추는 단계를 만든다. 이번 단계에서는 점수를 계산하지 않는다.
`score`는 계속 `0`이다.

`max_iterations`가 `5`인 이유: 사용자 결정에 따라 충돌·고정·줄 제거·게임오버 네 덩어리를
한 장에 담아 완료 조건이 27개다.

## 2. 기술과 파일 구조

- `CLAUDE.md`의 기술 제약을 그대로 따른다. **파일을 추가하지 않는다.**

| 파일 | 이번 SPEC에서 하는 일 |
| --- | --- |
| `game.js` | 충돌 판정, 고정, 줄 탐색·제거, 착지 순서, 게임오버. 전부 순수 함수 |
| `main.js` | 예약 틱 가드, 게임오버 시 타이머 정지, 생성기 주입 연결 |
| `test.js` | 테스트 케이스 추가 |
| `index.html` | 변경 없음 |
| `style.css` | 변경 없음 |
| `test.html` | 변경 없음 |

- 고정된 블록도 조각 종류 문자열로 보드에 저장하므로 기존 `.cell-I` ~ `.cell-L` 색 규칙이
  그대로 적용된다. 새 CSS가 필요 없다.

## 3. 화면 정의

- SPEC_00 §3의 텍스트와 `MEMORY.md` D5의 배치(보드 중앙, 상단 HUD, 네온 테마)를
  그대로 유지한다. 새로운 화면 요소나 문구를 추가하지 않는다.
- 보드에 고정된 블록은 자기 조각 종류의 색으로 계속 보인다.
- 완성된 줄이 사라지면 그 위의 블록이 사라진 줄 수만큼 내려온다. 제거 애니메이션은 없다.
- `제거한 줄:` 값이 지운 줄 수만큼 올라간다.
- `점수:` 값은 계속 `0`이다.
- 게임오버가 되면 `게임 상태:` 값이 `GAME_OVER`가 된다.

## 4. 상태와 공개 함수

### 4-1. 유지되는 계약

`globalThis.TetrisGame`, `BOARD_WIDTH`, `BOARD_HEIGHT`, `GAME_STATUS`,
`TETROMINO_TYPES`, `TETROMINOES`, `createEmptyBoard()`, `createInitialState()`,
`toRenderBoard()`, `startGame()`, 그리고 상태 필드
`board` / `score` / `lines` / `status` / `piece` / `randomizer`.

`board`의 값은 빈 칸이면 `0`, 고정된 칸이면 조각 종류 문자열(`'I'` ~ `'L'`)이다.

`GAME_STATUS.LANDED`는 SPEC_00 §4가 다섯 상태를 요구하므로 상수로 남긴다. **이 SPEC부터
어떤 함수도 `LANDED`를 반환하지 않는다.** 착지는 한 틱 안에서 끝나고 상태는 `PLAYING`을
유지한다.

### 4-2. 계약이 바뀌는 함수

| 이름 | 변경 내용 |
| --- | --- |
| `movePiece(state, dx, dy)` | 경계 검사에 **고정 블록 겹침 검사**를 더한다. 겹치면 상태를 그대로 반환 |
| `rotatePiece(state)` | 같음. 회전 결과가 고정 블록과 겹치면 회전하지 않는다 |
| `spawnPiece(state)` | 놓을 자리가 이미 막혀 있으면 `piece: null`, `status: GAME_OVER`인 상태를 반환한다 |
| `stepDown(state)` | 내려갈 수 없을 때 `LANDED`로 바꾸지 않고 **착지 순서 전체**(4-4)를 수행한다 |

### 4-3. 추가되는 공개 함수

모두 순수 함수이며 DOM·타이머에 의존하지 않는다. 인자를 제자리에서 고치지 않는다.

| 이름 | 계약 |
| --- | --- |
| `canPlace(board, piece)` | 조각이 채운 칸이 모두 `0 <= col < 10`, `row < 20` 안에 있고 `board`의 해당 칸이 전부 `0`이면 `true`, 아니면 `false` |
| `lockPiece(state)` | 조각이 채운 칸을 `board`에 조각 종류 문자열로 쓰고 `piece`를 `null`로 만든 새 상태를 반환한다 |
| `findFullRows(board)` | 열 열 칸이 모두 `0`이 아닌 행의 번호를 오름차순 배열로 반환한다. 없으면 빈 배열 |
| `clearRows(board, rows)` | `rows`의 행을 모두 뺀 뒤 맨 위에 빈 행을 그만큼 채운 새 20×10 보드를 반환한다 |
| `resolveLanding(state)` | 4-4의 착지 순서를 한 번에 수행한 새 상태를 반환한다 |

### 4-4. 착지 순서

`stepDown()`이 조각을 한 칸 내릴 수 없다고 판정하면 `resolveLanding()`이 아래를 이 차례로
수행한다. 순서를 바꾸지 않는다.

1. 현재 조각을 보드에 고정한다 (`lockPiece`)
2. 완성된 줄을 모두 탐색한다 (`findFullRows`)
3. 찾은 줄을 **동시에** 제거하고 위쪽 행을 아래로 압축한다 (`clearRows`)
4. 제거한 줄 수를 `lines`에 더한다
5. 다음 조각을 생성한다 (`spawnPiece`)
6. 생성 자리가 막혀 있으면 `status`를 `GAME_OVER`로, `piece`를 `null`로 둔다

`resolveLanding()`은 조각을 한 번만 고정하고 한 번만 생성한다. 생성에 실패해도 다시
시도하지 않는다.

### 4-5. 게임오버 규칙

- `status`가 `GAME_OVER`이면 `movePiece()`, `rotatePiece()`, `stepDown()`은 받은 상태를
  그대로 돌려준다.
- `main.js`는 `GAME_OVER`가 되면 `stopTimer()`를 부른다.
- **예약 틱 무시** — `onTick()`은 진입하자마자 `status !== PLAYING`이면 타이머를 멈추고
  즉시 반환한다. 이미 큐에 들어간 콜백이 한 번 더 도는 경우를 막는다.
- 키 입력은 SPEC_01대로 `status !== PLAYING`이면 무시하므로 `GAME_OVER`에서 자동으로
  듣지 않는다.

### 4-6. 재시작

`startGame()`은 SPEC_01의 D4대로 `READY`·`PLAYING`·`GAME_OVER` 어느 상태에서든 빈 보드,
`score 0`, `lines 0`, `status PLAYING`으로 되돌리고 조각을 하나 생성한다.
`main.js`는 시작 버튼을 누를 때 기존 타이머를 정리한 뒤 새로 건다.

### 4-7. 수동 검증을 위한 생성기 연결

`main.js`는 상태를 만들 때 `globalThis.TETRIS_RANDOMIZER`가 함수면 그것을
`createInitialState()`의 `randomizer`로 넘긴다. 함수가 아니면 기본 생성기를 쓴다.

이것은 새 기능이 아니라 SPEC_01이 요구한 "원하는 블록을 선택할 수 있게" 하는 주입 지점을
페이지에 연결하는 접합부다. 이것이 없으면 무작위 조각으로 한 줄을 채워야 해서 §8의 수동
검증이 사실상 불가능하다. 화면에 보이는 요소는 늘지 않는다.

## 5. 범위 밖

- 점수 계산
- 하드 드롭(Space)과 일시정지(P)
- 리더보드와 브라우저 저장
- 레벨과 낙하 속도 변화
- 다음 조각 미리보기, 홀드
- 벽킥
- 줄 제거 애니메이션과 효과음
- lock delay(착지 후 유예 시간)
- T-스핀 판정
- 외부 배포

## 6. 완료 조건

- [ ] `canPlace()`가 고정 블록과 겹치는 위치에 `false`를 반환한다. (AUTO)
- [ ] `canPlace()`가 빈 보드의 유효한 위치에 `true`를 반환한다. (AUTO)
- [ ] 고정 블록과 겹치는 `movePiece()`는 조각 위치를 바꾸지 않는다. (AUTO)
- [ ] 고정 블록과 겹치는 `rotatePiece()`는 행렬과 위치를 바꾸지 않는다. (AUTO)
- [ ] `lockPiece()`가 조각이 덮은 칸에 조각 종류 문자열을 쓴다. (AUTO)
- [ ] `lockPiece()` 후 `piece`가 `null`이다. (AUTO)
- [ ] `lockPiece()`가 인자로 받은 보드를 바꾸지 않는다. (AUTO)
- [ ] `findFullRows()`가 가득 찬 행 번호를 오름차순으로 모두 반환한다. (AUTO)
- [ ] `findFullRows()`가 가득 찬 행이 없으면 빈 배열을 반환한다. (AUTO)
- [ ] `clearRows()`가 지정한 행을 보드에서 없앤다. (AUTO)
- [ ] `clearRows()` 후 지운 줄 위에 있던 행이 아래로 내려온다. (AUTO)
- [ ] `clearRows()` 후에도 보드가 20행 10열이다. (AUTO)
- [ ] 두 줄이 동시에 완성되면 한 번에 모두 제거된다. (AUTO)
- [ ] 제거한 줄 수가 `lines`에 누적된다. (AUTO)
- [ ] 착지 처리 후 새 조각이 `row 0`에 생성된다. (AUTO)
- [ ] 착지 처리 후 `status`가 `PLAYING`이다. (AUTO)
- [ ] 어떤 함수도 `LANDED`를 반환하지 않는다. (AUTO)
- [ ] 생성 위치가 막혀 있으면 `status`가 `GAME_OVER`가 된다. (AUTO)
- [ ] `GAME_OVER`가 되면 `piece`가 `null`이다. (AUTO)
- [ ] `GAME_OVER`에서 `stepDown()`이 상태를 바꾸지 않는다. (AUTO)
- [ ] `GAME_OVER`에서 `movePiece()`가 상태를 바꾸지 않는다. (AUTO)
- [ ] `startGame()`이 `GAME_OVER`에서 빈 보드로 되돌린다. (AUTO)
- [ ] `score`가 어떤 경우에도 `0`을 유지한다. (AUTO)
- [ ] `test.html`을 브라우저에서 직접 열었을 때 모든 테스트가 통과한다. (HUMAN)
- [ ] `index.html`을 브라우저에서 직접 열었을 때 콘솔 오류가 없다. (HUMAN)
- [ ] 조각이 쌓인 블록 위에 멈추고 곧바로 새 조각이 나타난다. (HUMAN)
- [ ] 쌓인 블록을 방향키로 뚫고 지나갈 수 없다. (HUMAN)
- [ ] 한 줄이 가득 차면 그 줄이 사라지고 위쪽이 내려온다. (HUMAN)
- [ ] `제거한 줄:` 표시가 지운 줄 수만큼 올라간다. (HUMAN)
- [ ] 위까지 쌓이면 게임 상태가 `GAME_OVER`로 표시되고 낙하가 멈춘다. (HUMAN)
- [ ] `GAME_OVER`에서 방향키를 눌러도 화면이 변하지 않는다. (HUMAN)
- [ ] `GAME_OVER`에서 시작 버튼을 누르면 빈 보드로 다시 시작한다. (HUMAN)

AUTO 23개, HUMAN 9개, 합계 32개.

## 7. 자동 검증

`test.js`에 케이스를 추가한다. 새 함수가 전부 순수 함수라 DOM 없이 확인할 수 있다.
보드를 직접 조립해 결정론적으로 검증한다.

```js
var state = TetrisGame.createInitialState({ randomizer: function () { return 'O'; } });
state.board[19] = ['I','I','I','I','I','I','I','I',0,0];
```

확인할 것:

- 고정 칸 위에 조각을 놓으려 할 때 `canPlace()`가 `false`
- 고정 블록이 오른쪽에 있을 때 오른쪽 `movePiece()`가 무시되는지
- 고정 블록 때문에 회전이 막히는지
- `lockPiece()` 후 보드의 해당 칸이 조각 종류 문자열인지, `piece`가 `null`인지
- `lockPiece()`가 원본 상태의 보드를 바꾸지 않았는지
- 한 행만 가득 찼을 때 `findFullRows()`가 `[19]`
- 두 행이 가득 찼을 때 `findFullRows()`가 `[18, 19]`
- `clearRows(board, [19])` 후 18행에 있던 값이 19행으로 내려왔는지
- `clearRows()` 결과가 20행이고 각 행이 10칸인지
- `clearRows()` 결과의 맨 위 행이 전부 `0`인지
- 19행을 9칸 채운 뒤 마지막 칸을 메우는 착지에서 `lines`가 `1` 늘어나는지
- 두 줄 동시 완성에서 `lines`가 `2` 늘어나는지
- 착지 후 `status`가 `PLAYING`이고 새 `piece`의 `row`가 `0`인지
- 0행을 가득 채운 보드에서 `spawnPiece()`가 `GAME_OVER`와 `piece: null`을 주는지
- `GAME_OVER` 상태에 `stepDown()`, `movePiece()`를 불러도 같은 값인지
- `startGame()` 후 보드의 모든 칸이 `0`이고 `lines`가 `0`인지
- 위 모든 경로에서 `score`가 `0`인지

## 8. 수동 검증

무작위 조각으로 한 줄을 채우려면 시간이 너무 걸린다. 브라우저 콘솔에서 생성기를 고정한 뒤
시작한다.

```js
globalThis.TETRIS_RANDOMIZER = function () { return 'I'; };
```

1. `index.html`을 열고 콘솔 오류가 없는지 확인한다.
2. 위 한 줄을 콘솔에 입력한 뒤 `시작`을 누른다.
3. 첫 조각을 왼쪽 끝까지 옮겨 바닥에 닿게 두고, 멈춘 자리에 그대로 고정되는지 확인한다.
4. 고정되자마자 새 조각이 위쪽 가운데에 나타나는지 확인한다.
5. 두 번째 조각을 첫 조각 위로 옮겨 바닥이 아니라 **쌓인 블록 위**에 멈추는지 확인한다.
6. 쌓인 블록 쪽으로 방향키를 눌러 뚫고 들어가지 않는지 확인한다.
7. `I` 조각 두 개와 나머지로 19행 열 칸을 모두 채워 그 줄이 사라지는지 확인한다.
8. 줄이 사라진 뒤 위에 있던 블록이 한 칸 내려오는지 확인한다.
9. `제거한 줄:` 값이 `1`로 올라갔는지 확인한다.
10. `점수:` 값이 여전히 `0`인지 확인한다.
11. 조각을 계속 쌓아 맨 위까지 채우고 `게임 상태:`가 `GAME_OVER`로 바뀌는지 확인한다.
12. `GAME_OVER` 이후 조각이 더 내려오지 않는지 5초간 확인한다.
13. `GAME_OVER`에서 방향키를 눌러도 화면이 변하지 않는지 확인한다.
14. `시작` 버튼을 눌러 빈 보드, `제거한 줄: 0`으로 돌아가는지 확인한다.
15. `test.html`을 열어 통과·실패 개수를 확인한다.

## 9. 안전과 정지 조건

- 프로젝트 폴더 밖 파일을 만들거나 수정하지 않는다.
- 외부 배포와 유료 서비스를 사용하지 않는다.
- 같은 실패가 두 번 반복되면 중단한다.
- 이 문서에 없는 화면·기술 결정을 해야 하면 추측하지 않고 `[사람 확인 필요]`로 보고한다.
  단 색상·여백 같은 순수 시각 속성은 `MEMORY.md`의 D1에 따라 구현자가 정한다.

### 이 SPEC 고유의 항목

- **SPEC_01에서 무효가 되는 항목.** `/spec-run`은 회차 1에서 아래를 `MEMORY.md`에
  기록해야 한다. 셋 다 실패로 기록하지 않는다.
  1. SPEC_01 완료 조건 11 (AUTO, 통과) "조각의 가장 아래 칸이 19행일 때 `stepDown()`이
     `status`를 `LANDED`로 바꾼다" → SPEC_02의 착지 순서가 **대체**한다.
  2. SPEC_01 완료 조건 23 (HUMAN) "조각이 바닥에 닿으면 게임 상태가 `LANDED`가 되고 더
     내려가지 않는다" → **대체**한다.
  3. SPEC_01 §4-5 "`LANDED` 이후 방향키를 무시하고 새 조각을 생성하지 않는다"
     → **대체**한다. 이제 착지하면 곧바로 다음 조각이 나온다.
- SPEC_01 완료 조건 8과 10(경계 밖 이동·회전 무시)은 **여전히 유효하며 확장**된다.
  경계 검사에 고정 블록 겹침이 더해질 뿐이다.
- **무한 루프 금지.** `resolveLanding()`은 고정 한 번, 생성 한 번으로 끝낸다. 생성 실패
  시 재시도 없이 즉시 `GAME_OVER`로 종료한다. 착지 처리 안에서 다시 착지를 부르지 않는다.
- **예약 틱.** `GAME_OVER`가 된 뒤 이미 예약된 콜백이 한 번 더 도는 것을 `onTick()`
  진입 가드로 막는다. 가드 없이 `stopTimer()`만 부르면 마지막 한 틱이 새는 경우가 있다.
- **회차 예산 경고.** 완료 조건 32개, HUMAN 9건이라 `max_iterations: 5`로도 부족할 수
  있다. 회차 3을 마친 시점에 `미확인` 또는 `실패`가 5건 이상이면 이 SPEC을 나누자고
  사용자에게 제안하고 정지한다.
- **확인 백로그.** SPEC_01 HUMAN 11건, SPEC_00 재확인 5건, S1~S3이 아직 미확인이다.
  SPEC_02 회차 1의 사람 확인 요청은 이것들과 합쳐서 한 번에 낸다.
