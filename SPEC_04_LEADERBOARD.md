---
id: SPEC_04
title: 로컬 리더보드
depends_on: [SPEC_00, SPEC_01, SPEC_02, SPEC_03]
max_iterations: 5
---

# SPEC 04 — 로컬 리더보드

> **선행 조건 주의 — 확인 백로그 38건**
> SPEC_00 정적 화면 확인 이후로 브라우저 확인이 한 번도 들어오지 않았다. 키 입력, 타이머,
> 렌더링, CSS는 실물로 확인된 적이 없다. 이 SPEC은 처음으로 **새 화면 요소와 입력 폼**을
> 추가하므로, 밀린 확인을 먼저 끝내는 것을 강하게 권장한다.

## 1. 목표

게임이 끝나면 최종 점수와 제거한 줄 수를 보여주고, 이름을 받아 브라우저 저장소에 상위 10개
기록을 남기는 로컬 리더보드를 만든다. 이번 단계에서는 서버·온라인 순위·로그인·기기 간
동기화를 구현하지 않는다.

`max_iterations`가 `5`인 이유: 사용자 결정에 따라 데이터 계층과 UI 계층을 한 장에 담아
완료 조건이 29개이고, 이 프로젝트에서 처음으로 입력 폼과 브라우저 저장소를 다룬다.

## 2. 기술과 파일 구조

- `CLAUDE.md`의 기술 제약을 그대로 따른다. **파일을 추가하지 않는다.**
- `localStorage`는 브라우저 기본 API이므로 외부 라이브러리에 해당하지 않는다.

| 파일 | 이번 SPEC에서 하는 일 |
| --- | --- |
| `game.js` | 이름 검증, 정렬, 직렬화, 저장소 읽기·쓰기. 전부 순수 함수 |
| `main.js` | 게임오버 패널 표시·숨김, 폼 처리, 목록 렌더, 초기화 |
| `index.html` | 게임오버 패널 마크업 추가 |
| `style.css` | 패널·폼·목록 스타일 |
| `test.js` | 테스트 케이스 추가 |
| `test.html` | 변경 없음 |

`game.js`의 저장소 함수는 저장소 객체를 **인자로 받는다**. 전역 `localStorage`를 직접
참조하면 node에서 검증할 수 없다.

## 3. 화면 정의

`MEMORY.md`의 D5(보드 중앙·네온)와 D6(로고 점멸)은 그대로 유지한다. 보드·HUD·시작 버튼·
조작 안내는 바뀌지 않는다.

### 3-1. 게임오버 패널

`게임 상태:`가 `GAME_OVER`일 때만 보인다. 그 밖의 상태에서는 화면에 없다.
보드 아래, 조작 안내 위에 놓는다.

패널에 나타나는 텍스트는 다음과 같다.

| 요소 | 텍스트 |
| --- | --- |
| 최종 점수 레이블 | `최종 점수:` |
| 제거한 줄 레이블 | `제거한 줄:` |
| 이름 입력 자리표시 | `이름` |
| 저장 버튼 | `저장` |
| 초기화 버튼 | `기록 초기화` |
| 기록이 없을 때 | `아직 기록이 없습니다.` |

목록의 각 줄은 `순위. 이름 · 점수점 · 줄수줄` 형식이다.
예: `1. 홍길동 · 1240점 · 5줄`

### 3-2. 메시지

저장 버튼을 눌렀을 때 패널의 메시지 자리에 다음 중 하나가 나타난다.

| 상황 | 메시지 |
| --- | --- |
| 이름이 비어 있음 | `이름을 입력하세요.` |
| trim 후 2자 미만 | `이름은 2자 이상이어야 합니다.` |
| trim 후 10자 초과 | `이름은 10자 이하여야 합니다.` |
| 허용되지 않는 문자 | `이름에는 한글, 영문, 숫자만 쓸 수 있습니다.` |
| 저장 성공 | `저장했습니다.` |
| 이미 저장한 게임 | `이 게임은 이미 저장했습니다.` |
| 저장소를 쓸 수 없음 | `기록을 저장할 수 없습니다.` |

이 문구들은 SPEC_00 §3이 정한 `점수:` · `제거한 줄:` 어휘를 따라 만든 것이다.
사용자가 다른 문구를 원하면 지시로 바꾼다.

### 3-3. 입력 제한

이름 입력 칸에 `maxlength` 속성을 두지 않는다. 길이 제한을 마크업으로 막으면 10자 초과
입력 자체가 불가능해져 `이름은 10자 이하여야 합니다.` 메시지를 확인할 수 없다.

## 4. 상태와 공개 함수

### 4-1. 유지되는 계약

SPEC_00~03의 상수와 함수, 상태 필드
`board` / `score` / `lines` / `status` / `piece` / `randomizer`를 그대로 유지한다.
**게임 상태 객체에 리더보드 관련 필드를 추가하지 않는다.** 리더보드는 게임 상태와 분리한다.

### 4-2. 추가되는 공개 값

| 이름 | 값 |
| --- | --- |
| `STORAGE_KEY` | `'tetris-loop.leaderboard.v1'` |
| `NAME_MIN_LENGTH` | `2` |
| `NAME_MAX_LENGTH` | `10` |
| `LEADERBOARD_LIMIT` | `10` |

### 4-3. 기록의 모양

| 필드 | 타입 | 설명 |
| --- | --- | --- |
| `id` | 문자열 | 기록을 구분하는 값 |
| `name` | 문자열 | trim된 이름 |
| `score` | 숫자 | 최종 점수 |
| `clearedLines` | 숫자 | 제거한 줄 수 |
| `playedAt` | 문자열 | ISO 8601 (`new Date().toISOString()`) |

`playedAt`을 ISO 8601 문자열로 두는 이유: 사전순 비교가 곧 시간순 비교라 정렬 규칙이
문자열 하나로 끝난다.

### 4-4. 추가되는 공개 함수

모두 순수 함수다. 시간·난수·전역 저장소에 직접 접근하지 않는다.

| 이름 | 계약 |
| --- | --- |
| `validateName(raw)` | `{ valid, name, reason }`. `name`은 `raw`를 trim한 값. 유효하면 `reason`은 빈 문자열 |
| `createEntry(name, score, clearedLines, id, playedAt)` | 4-3의 다섯 필드를 가진 새 객체 |
| `sortEntries(entries)` | 4-5 규칙으로 정렬한 **새 배열**. 인자를 바꾸지 않는다 |
| `addEntry(entries, entry)` | 기록을 더하고 정렬한 뒤 상위 `LEADERBOARD_LIMIT`개만 남긴 새 배열 |
| `parseEntries(text)` | 문자열을 기록 배열로. 조금이라도 어긋나면 빈 배열 |
| `loadLeaderboard(storage)` | `storage.getItem(STORAGE_KEY)`를 읽어 `parseEntries()`에 넘긴다. 예외는 빈 배열로 흡수 |
| `saveLeaderboard(storage, entries)` | `JSON.stringify`해서 `storage.setItem()`. 성공하면 `true`, 예외면 `false` |
| `clearLeaderboard(storage)` | `storage.removeItem(STORAGE_KEY)`. 성공하면 `true`, 예외면 `false` |

`createEntry`가 `id`와 `playedAt`을 인자로 받는 이유: 함수 안에서 `Date.now()`나 난수를
부르면 결과가 실행할 때마다 달라져 자동 검증이 불가능해진다. 실제 값은 `main.js`가 만든다.

### 4-5. 이름 검증 규칙

1. `raw`를 trim한다.
2. trim 결과가 빈 문자열이면 `이름을 입력하세요.`
3. 길이가 `NAME_MIN_LENGTH` 미만이면 `이름은 2자 이상이어야 합니다.`
4. 길이가 `NAME_MAX_LENGTH` 초과면 `이름은 10자 이하여야 합니다.`
5. 허용 문자 밖의 글자가 하나라도 있으면 `이름에는 한글, 영문, 숫자만 쓸 수 있습니다.`
6. 모두 통과하면 `{ valid: true, name, reason: '' }`

**허용 문자** — 한글 완성형 `가`~`힣`, 한글 자모 `ㄱ`~`ㅎ`·`ㅏ`~`ㅣ`, 영문 `A`~`Z`·`a`~`z`,
숫자 `0`~`9`. 공백과 그 밖의 모든 문자는 허용하지 않는다.

**길이 세는 법** — 허용 문자는 모두 BMP 안의 한 코드 단위라 `문자열.length`가 곧 보이는
글자 수다. 허용 문자 검사를 길이 검사보다 뒤에 두면 이모지 같은 대리쌍이 2자로 세어질 수
있으므로, 위 순서(길이 → 문자)를 지키고 문자 검사에서 걸러낸다.

### 4-6. 정렬 규칙

앞선 기준이 같을 때만 다음 기준으로 넘어간다.

1. `score` **내림차순**
2. `name` **오름차순** — 코드 단위 비교(`<`, `>`)를 쓴다. `localeCompare`는 실행 환경의
   로케일에 따라 결과가 달라져 자동 검증이 흔들린다.
3. `playedAt` **오름차순** — 먼저 플레이한 기록이 위로 온다.
4. 셋 다 같으면 **기존 순서**를 유지한다. `Array.prototype.sort`가 안정 정렬이므로
   비교 함수가 `0`을 반환하면 자동으로 보장된다.

### 4-7. 저장 규칙

- 저장 키는 `STORAGE_KEY` 하나뿐이다. 다른 키를 쓰지 않는다.
- 저장할 때 항상 상위 `LEADERBOARD_LIMIT`개로 잘라서 쓴다.
- **손상 복구** — `parseEntries()`는 다음 경우에 모두 빈 배열을 반환한다.
  - `null`이거나 문자열이 아닐 때
  - `JSON.parse`가 예외를 던질 때
  - 파싱 결과가 배열이 아닐 때
  - 항목 중 하나라도 객체가 아니거나, 4-3의 다섯 필드가 없거나, `score`·`clearedLines`가
    숫자가 아닐 때
- 저장소 접근 자체가 예외를 던지면(사생활 모드, `file://` 제약 등) 읽기는 빈 배열,
  쓰기는 `false`로 흡수하고 게임은 계속 동작한다.

### 4-8. 한 게임 한 번만 저장

`main.js`가 `savedThisGame` 플래그를 둔다.

- `startGame()`으로 새 게임을 시작할 때 `false`로 되돌린다.
- 저장에 성공하면 `true`로 바꾸고 이름 입력과 저장 버튼을 비활성화한다.
- `true`인 상태에서 저장을 시도하면 `이 게임은 이미 저장했습니다.`를 표시하고 저장하지
  않는다.

이 플래그는 게임 상태 객체가 아니라 `main.js`의 지역 변수다.

### 4-9. 초기화

`기록 초기화` 버튼을 누르면 브라우저 `confirm()`으로 확인을 받는다. 확인했을 때만
`clearLeaderboard()`를 부르고 목록을 다시 그린다. 취소하면 아무 일도 하지 않는다.

## 5. 범위 밖

- 서버와 온라인 순위
- 로그인과 사용자 계정
- 여러 기기 간 동기화
- 기록 내보내기·가져오기
- 기록 개별 삭제와 수정
- 순위 변동 애니메이션
- 레벨과 낙하 속도 변화
- 다음 조각 미리보기, 홀드, 일시정지
- 외부 배포

## 6. 완료 조건

- [ ] `STORAGE_KEY`가 `'tetris-loop.leaderboard.v1'`이다. (AUTO)
- [ ] `validateName()`이 이름 앞뒤 공백을 제거한다. (AUTO)
- [ ] trim 후 빈 이름을 거부하고 `이름을 입력하세요.`를 준다. (AUTO)
- [ ] 1자 이름을 거부하고 `이름은 2자 이상이어야 합니다.`를 준다. (AUTO)
- [ ] 11자 이름을 거부하고 `이름은 10자 이하여야 합니다.`를 준다. (AUTO)
- [ ] 허용되지 않는 문자를 거부하고 그 이유를 준다. (AUTO)
- [ ] 한글 완성형·자모·영문·숫자로 된 이름을 통과시킨다. (AUTO)
- [ ] `createEntry()`가 4-3의 다섯 필드를 가진 객체를 만든다. (AUTO)
- [ ] `sortEntries()`가 점수 내림차순으로 정렬한다. (AUTO)
- [ ] 점수가 같으면 이름 오름차순으로 정렬한다. (AUTO)
- [ ] 점수와 이름이 같으면 `playedAt` 오름차순으로 정렬한다. (AUTO)
- [ ] 세 기준이 모두 같으면 기존 순서를 유지한다. (AUTO)
- [ ] `sortEntries()`가 인자로 받은 배열을 바꾸지 않는다. (AUTO)
- [ ] `addEntry()`가 결과를 열 개 이하로 유지한다. (AUTO)
- [ ] 열한 번째 기록이 점수가 낮으면 목록에서 밀려난다. (AUTO)
- [ ] `parseEntries()`가 JSON이 아닌 문자열에 빈 배열을 준다. (AUTO)
- [ ] `parseEntries()`가 배열이 아닌 JSON에 빈 배열을 준다. (AUTO)
- [ ] `parseEntries()`가 필드가 빠진 항목이 있으면 빈 배열을 준다. (AUTO)
- [ ] `loadLeaderboard()`가 저장소 예외를 빈 배열로 흡수한다. (AUTO)
- [ ] `saveLeaderboard()`가 저장소 예외에 `false`를 준다. (AUTO)
- [ ] `saveLeaderboard()`가 `STORAGE_KEY`에 쓴다. (AUTO)
- [ ] `clearLeaderboard()`가 `STORAGE_KEY`를 지운다. (AUTO)
- [ ] `test.html`을 브라우저에서 직접 열었을 때 모든 테스트가 통과한다. (HUMAN)
- [ ] `index.html`을 브라우저에서 직접 열었을 때 콘솔 오류가 없다. (HUMAN)
- [ ] 게임오버 전에는 게임오버 패널이 화면에 없다. (HUMAN)
- [ ] 게임오버가 되면 패널에 최종 점수와 제거한 줄 수가 표시된다. (HUMAN)
- [ ] 유효하지 않은 이름으로 저장하면 이유가 표시되고 목록이 늘지 않는다. (HUMAN)
- [ ] 유효한 이름으로 저장하면 목록에 그 기록이 나타난다. (HUMAN)
- [ ] 같은 게임에서 두 번째 저장이 거부된다. (HUMAN)
- [ ] 페이지를 새로 고쳐도 저장한 기록이 남아 있다. (HUMAN)
- [ ] `기록 초기화`에서 취소하면 목록이 그대로다. (HUMAN)
- [ ] `기록 초기화`에서 확인하면 목록이 빈다. (HUMAN)
- [ ] 시작 버튼으로 새 게임을 시작하면 패널이 사라진다. (HUMAN)

AUTO 22개, HUMAN 11개, 합계 33개.

## 7. 자동 검증

`test.js`에 케이스를 추가한다. 저장소 함수가 저장소 객체를 인자로 받으므로 가짜 저장소를
만들어 DOM 없이 검증할 수 있다.

```js
function fakeStorage(initial) {
  var data = initial;
  return {
    getItem: function () { return data; },
    setItem: function (k, v) { data = v; },
    removeItem: function () { data = null; }
  };
}

function throwingStorage() {
  return {
    getItem: function () { throw new Error('blocked'); },
    setItem: function () { throw new Error('blocked'); },
    removeItem: function () { throw new Error('blocked'); }
  };
}
```

확인할 것:

- `STORAGE_KEY`, `NAME_MIN_LENGTH`, `NAME_MAX_LENGTH`, `LEADERBOARD_LIMIT` 값
- `'  홍길동  '`의 trim 결과가 `'홍길동'`인지
- `''`, `'  '`, `'가'`, 11자 이름, `'홍길동!'`, `'hong gil'`(공백 포함)의 거부와 이유
- `'가나'`, `'ㅋㅋ'`, `'Player1'`, `'abcdefghij'`(10자)의 통과
- `createEntry()` 결과의 다섯 필드
- 점수만 다른 세 기록의 정렬
- 점수가 같고 이름이 다른 기록의 정렬
- 점수·이름이 같고 `playedAt`이 다른 기록의 정렬
- 셋 다 같은 두 기록이 넣은 순서를 유지하는지
- `sortEntries()` 호출 뒤 원본 배열의 순서가 그대로인지
- 열한 개를 넣었을 때 결과 길이가 `10`인지
- 가장 낮은 점수가 밀려났는지
- `parseEntries('{')`, `parseEntries('{"a":1}')`, `parseEntries(null)`,
  `parseEntries('[{"id":"1"}]')`가 모두 빈 배열인지
- 정상 JSON을 넣었을 때 기록이 복원되는지
- 가짜 저장소로 저장한 뒤 읽으면 같은 기록이 나오는지
- `throwingStorage()`로 읽으면 빈 배열, 쓰면 `false`인지
- `saveLeaderboard()`가 넘긴 키가 `STORAGE_KEY`인지

## 8. 수동 검증

게임오버까지 가야 하므로 생성기를 고정하고 하드 드롭으로 빠르게 쌓는다.

```js
globalThis.TETRIS_RANDOMIZER = function () { return 'O'; };
```

1. `index.html`을 열고 콘솔 오류가 없는지 확인한다.
2. 시작 전에는 게임오버 패널이 화면에 없는지 확인한다.
3. `시작`을 누르고 `Space`를 반복해 맨 위까지 쌓아 게임오버를 만든다.
4. 패널이 나타나고 `최종 점수:`와 `제거한 줄:` 값이 HUD와 같은지 확인한다.
5. 이름을 비운 채 `저장`을 눌러 `이름을 입력하세요.`가 뜨는지 확인한다.
6. `가`만 입력하고 저장해 `이름은 2자 이상이어야 합니다.`가 뜨는지 확인한다.
7. 11자를 입력하고 저장해 `이름은 10자 이하여야 합니다.`가 뜨는지 확인한다.
8. `홍길동!`을 입력하고 저장해 문자 제한 메시지가 뜨는지 확인한다.
9. `홍길동`을 입력하고 저장해 `저장했습니다.`와 함께 목록에 나타나는지 확인한다.
10. 저장 버튼을 다시 눌러 `이 게임은 이미 저장했습니다.`가 뜨는지 확인한다.
11. 페이지를 새로 고치고 게임오버까지 다시 가서 목록에 이전 기록이 남아 있는지 확인한다.
12. `기록 초기화`를 누르고 **취소**해 목록이 그대로인지 확인한다.
13. `기록 초기화`를 누르고 **확인**해 `아직 기록이 없습니다.`가 되는지 확인한다.
14. `시작`을 눌러 패널이 사라지는지 확인한다.
15. `test.html`을 열어 통과·실패 개수를 확인한다.

## 9. 안전과 정지 조건

- 프로젝트 폴더 밖 파일을 만들거나 수정하지 않는다.
- 외부 배포와 유료 서비스를 사용하지 않는다.
- 같은 실패가 두 번 반복되면 중단한다.
- 이 문서에 없는 화면·기술 결정을 해야 하면 추측하지 않고 `[사람 확인 필요]`로 보고한다.
  단 색상·여백 같은 순수 시각 속성은 `MEMORY.md`의 D1에 따라 구현자가 정한다.

### 이 SPEC 고유의 항목

- **`file://`에서 `localStorage`가 막힐 수 있다.** `CLAUDE.md`는 개발 서버를 금지하고
  SPEC_00은 `index.html`을 직접 열라고 한다. 브라우저와 설정에 따라 `file://` 출처에서
  저장소 접근이 예외를 던지거나 페이지마다 격리될 수 있다. 이때는 **코드가 옳아도**
  완료 조건 "새로 고쳐도 기록이 남는다"가 실패한다.
  - 모든 저장소 접근을 `try`/`catch`로 감싸 게임이 멈추지 않게 한다.
  - 이 조건이 실패하면 코드를 고치기 전에 **먼저 다른 브라우저에서 확인**하고, 환경 문제로
    판명되면 `MEMORY.md`에 환경 제약으로 기록한 뒤 사용자에게 보고한다. 같은 수정을 두 번
    시도하지 않는다.
- **SPEC_00 §5의 "리더보드와 브라우저 저장"을 이 SPEC이 구현한다.** 후속 SPEC이 범위를
  넓히는 정상적인 진행이며 실패로 기록하지 않는다. `/spec-run`이 회차 1에서
  `MEMORY.md`에 기록한다.
- **화면 요소가 늘어난다.** 이 프로젝트에서 `index.html`에 새 요소를 추가하는 첫 SPEC이다.
  D5(중앙 배치)와 D6(로고 점멸)을 깨뜨리지 않는지, 390px에서 가로 스크롤이 생기지 않는지
  함께 확인한다.
- **게임 상태를 오염시키지 않는다.** 리더보드는 게임 상태 객체 바깥에 둔다. 상태 필드를
  늘리면 SPEC_00~03의 계약 검사가 깨진다.
- **`confirm()`은 자동 검증할 수 없다.** 초기화 확인은 `HUMAN` 항목으로만 판정한다.
- **회차 예산 경고.** 완료 조건 33개, HUMAN 11건이다. 회차 3을 마친 시점에 `미확인` 또는
  `실패`가 6건 이상이면 이 SPEC을 나누자고 사용자에게 제안하고 정지한다.
- **확인 백로그.** 앞선 SPEC의 미확인 38건이 남아 있다. SPEC_04 회차 1의 사람 확인 요청은
  이것들과 합쳐서 한 번에 낸다.
