---
id: SPEC_03
title: 점수와 하드 드롭
depends_on: [SPEC_00, SPEC_01, SPEC_02]
max_iterations: 4
---

# SPEC 03 — 점수와 하드 드롭

> **선행 조건 주의 — 확인 백로그 31건**
> `SPEC_01` HUMAN 10건, `SPEC_02` HUMAN 9건, `SPEC_00` D5 재확인 5건, S1~S3, F1~F4가
> 모두 `미확인`이다. 이 SPEC을 실행하기 전에 밀린 브라우저 확인을 끝내는 것을 권장한다.

## 1. 목표

줄을 지우고 조각을 떨어뜨릴 때 점수를 매기고, Space로 조각을 충돌 직전까지 한 번에
내리는 하드 드롭을 만든다. 이번 단계에서는 레벨과 속도 변화, 점수 배수를 구현하지 않는다.

`max_iterations`가 `4`인 이유: 사용자 결정에 따라 점수 체계와 하드 드롭을 한 장에 담아
완료 조건이 25개다.

## 2. 기술과 파일 구조

- `CLAUDE.md`의 기술 제약을 그대로 따른다. **파일을 추가하지 않는다.**

| 파일 | 이번 SPEC에서 하는 일 |
| --- | --- |
| `game.js` | 점수 상수와 계산, `softDrop`, `hardDrop`. 전부 순수 함수 |
| `main.js` | `↓`를 `softDrop`으로, `Space`를 `hardDrop`으로 연결 |
| `test.js` | 테스트 케이스 추가 |
| `index.html` | 변경 없음 |
| `style.css` | 변경 없음 |
| `test.html` | 변경 없음 |

## 3. 화면 정의

**이번 SPEC에서 화면 구성은 바뀌지 않는다.** 새 요소도 새 문구도 추가하지 않는다.

- `점수:` 값이 점수가 오를 때마다 즉시 바뀐다. 지금까지는 항상 `0`이었다.
- `제거한 줄:` 값은 SPEC_02대로 계속 동작한다.
- 조작 안내의 `Space 하드 드롭`은 SPEC_00 §3부터 이미 화면에 있었다. 이 SPEC이 그 문구를
  실제 동작과 일치시킨다.
- `MEMORY.md`의 D5(중앙 배치·네온)와 D6(로고 점멸)은 그대로 유지한다.

## 4. 상태와 공개 함수

### 4-1. 유지되는 계약

SPEC_00·01·02의 상수와 함수, 그리고 상태 필드
`board` / `score` / `lines` / `status` / `piece` / `randomizer`.
`startGame()`, `resolveLanding()`의 착지 순서, `GAME_OVER` 규칙 모두 유지한다.

`score`는 지금까지 항상 `0`이었다. 이 SPEC부터 실제 값을 갖는다.

### 4-2. 추가되는 공개 값

| 이름 | 값 |
| --- | --- |
| `LINE_SCORES` | `[0, 100, 300, 500, 800]` — 인덱스가 한 번에 지운 줄 수 |
| `SOFT_DROP_POINT` | `1` — 소프트 드롭으로 내린 칸 하나당 |
| `HARD_DROP_POINT` | `2` — 하드 드롭으로 내린 칸 하나당 |

### 4-3. 추가되는 공개 함수

| 이름 | 계약 |
| --- | --- |
| `lineScore(count)` | `LINE_SCORES[count]`. `count`가 배열 범위를 넘으면 마지막 값(`800`)을 반환한다 |
| `softDrop(state)` | 한 칸 내릴 수 있으면 내리고 `score`에 `SOFT_DROP_POINT`를 더한다. 내릴 수 없으면 점수를 주지 않고 착지 순서를 수행한다 |
| `hardDrop(state)` | 더 내려갈 수 없는 행까지 한 번에 내리고, 내려간 칸 수 × `HARD_DROP_POINT`를 `score`에 더한 뒤 **곧바로 착지 순서를 수행한다** |

### 4-4. 계약이 바뀌는 함수

| 이름 | 변경 |
| --- | --- |
| `resolveLanding(state)` | 지운 줄 수 `n`에 대해 `score`에 `lineScore(n)`을 더한다. 나머지 순서는 SPEC_02 §4-4 그대로 |
| `stepDown(state)` | **자동 낙하 전용이 된다.** 점수를 주지 않는다. 착지 시 `resolveLanding()`을 부르는 동작은 그대로 |

`↓` 키는 이제 `stepDown()`이 아니라 `softDrop()`을 부른다. 자동 낙하와 소프트 드롭을
같은 함수로 처리하면 자동 낙하에도 점수가 붙어 요구사항에 어긋난다.

### 4-5. 점수 규칙

- **줄 제거** — 한 번에 지운 줄 수에 따라 `100` / `300` / `500` / `800`.
  0줄이면 `0`이다. 조각이 네 칸이라 다섯 줄 이상은 생기지 않지만, 방어적으로 `800`을
  넘지 않는다.
- **소프트 드롭** — `↓`로 **실제로 내려간** 칸마다 `1`점. 내려가지 못하면 `0`점이다.
- **하드 드롭** — Space로 **실제로 내려간** 칸마다 `2`점. 이미 더 내려갈 수 없으면
  `0`칸 이동이므로 `0`점이고, 그래도 즉시 고정된다.
- **자동 낙하** — 800ms 타이머로 내려간 칸에는 점수를 주지 않는다.
- **합산** — 드롭 점수와 줄 제거 점수는 더한다.
  하드 드롭으로 `d`칸 내려가 `n`줄을 지웠다면 `2 × d + lineScore(n)`이다.
- **레벨 배수 없음** — 이 단계에서는 어떤 배수도 곱하지 않는다.

### 4-6. 입력 규칙

- `Space`는 `status`가 `PLAYING`이고 조각이 있을 때만 하드 드롭을 일으킨다.
- **`Space`에 `preventDefault()`를 반드시 부른다.** 시작 버튼에 포커스가 있는 상태에서
  Space를 누르면 브라우저가 버튼을 눌러 게임이 재시작된다. 이것을 막는다.
- `READY`와 `GAME_OVER`에서 `softDrop()`, `hardDrop()`은 받은 상태를 그대로 돌려준다.
- 하드 드롭은 타이머 주기를 리셋하지 않는다. SPEC_01의 `↓` 규칙과 같다.

### 4-7. 재시작

`startGame()`은 SPEC_02 §4-6 계약 그대로 빈 보드, `score 0`, `lines 0`, `PLAYING`으로
되돌린다. 점수와 줄 수가 둘 다 `0`이 되며, 보드 초기화도 기존 계약대로 함께 일어난다.

## 5. 범위 밖

- 레벨과 낙하 속도 변화
- 레벨에 따른 점수 배수
- 콤보, 백투백, T-스핀 보너스
- 소프트 드롭 중 조작에 대한 추가 점수
- 최고 점수 저장과 리더보드
- 점수 변화 애니메이션과 효과음
- 일시정지(P)
- 다음 조각 미리보기, 홀드
- 벽킥
- 외부 배포

## 6. 완료 조건

- [ ] `LINE_SCORES`가 `[0, 100, 300, 500, 800]`이다. (AUTO)
- [ ] `lineScore()`가 0~4줄에 대해 `0`, `100`, `300`, `500`, `800`을 반환한다. (AUTO)
- [ ] 한 줄을 지우면 `score`가 `100` 늘어난다. (AUTO)
- [ ] 두 줄을 동시에 지우면 `score`가 `300` 늘어난다. (AUTO)
- [ ] 네 줄을 동시에 지우면 `score`가 `800` 늘어난다. (AUTO)
- [ ] `softDrop()`이 조각을 한 칸 내린다. (AUTO)
- [ ] `softDrop()`으로 한 칸 내리면 `score`가 `1` 늘어난다. (AUTO)
- [ ] 내려갈 수 없을 때 `softDrop()`이 점수를 주지 않는다. (AUTO)
- [ ] 내려갈 수 없을 때 `softDrop()`이 착지 순서를 수행한다. (AUTO)
- [ ] 자동 낙하 `stepDown()`은 `score`를 늘리지 않는다. (AUTO)
- [ ] `hardDrop()`이 조각을 더 내려갈 수 없는 행까지 내린다. (AUTO)
- [ ] `hardDrop()`이 내려간 칸 수 × `2`를 `score`에 더한다. (AUTO)
- [ ] `hardDrop()` 후 조각이 보드에 고정된다. (AUTO)
- [ ] `hardDrop()` 후 새 조각이 생성된다. (AUTO)
- [ ] 0칸 하드 드롭은 `score`를 늘리지 않는다. (AUTO)
- [ ] 하드 드롭과 줄 제거가 함께 일어나면 두 점수가 더해진다. (AUTO)
- [ ] `startGame()`이 `score`를 `0`으로 되돌린다. (AUTO)
- [ ] `startGame()`이 `lines`를 `0`으로 되돌린다. (AUTO)
- [ ] `GAME_OVER`에서 `hardDrop()`이 상태를 바꾸지 않는다. (AUTO)
- [ ] `GAME_OVER`에서 `softDrop()`이 상태를 바꾸지 않는다. (AUTO)
- [ ] `test.html`을 브라우저에서 직접 열었을 때 모든 테스트가 통과한다. (HUMAN)
- [ ] `index.html`을 브라우저에서 직접 열었을 때 콘솔 오류가 없다. (HUMAN)
- [ ] 줄을 지우면 `점수:` 표시가 즉시 올라간다. (HUMAN)
- [ ] `↓`를 누를 때마다 `점수:`가 `1`씩 올라간다. (HUMAN)
- [ ] `Space`를 누르면 조각이 아래 끝까지 한 번에 내려간다. (HUMAN)
- [ ] `Space`를 눌러도 게임이 재시작되지 않는다. (HUMAN)
- [ ] 시작 버튼을 누르면 `점수:`와 `제거한 줄:`이 둘 다 `0`이 된다. (HUMAN)

AUTO 20개, HUMAN 7개, 합계 27개.

## 7. 자동 검증

`test.js`에 케이스를 추가한다. 새 함수가 전부 순수 함수라 DOM 없이 확인할 수 있다.
보드를 직접 조립하고 생성기를 고정해 결정론적으로 검증한다.

확인할 것:

- `LINE_SCORES` 배열의 값과 길이
- `lineScore(0)` ~ `lineScore(4)`, 그리고 범위를 넘는 값
- 19행을 아홉 칸 채우고 마지막 칸을 메우는 착지에서 `score`가 `100` 증가
- 18·19행을 동시에 채우는 착지에서 `score`가 `300` 증가
- 16~19행 네 줄을 세로 `I`로 동시에 채우는 착지에서 `score`가 `800` 증가
- `softDrop()` 한 번에 `piece.row`가 `1` 늘고 `score`가 `1` 느는지
- `softDrop()`을 다섯 번 부르면 `score`가 `5`인지
- 바닥에 붙은 조각에 `softDrop()`을 부르면 점수 없이 고정되는지
- 같은 자리에서 `stepDown()`을 부르면 `score`가 그대로인지
- 빈 보드에서 `hardDrop()` 후 조각이 바닥에 고정되는지
- 그때 `score`가 내려간 칸 수 × `2`인지
- 쌓인 블록 위에서 하드 드롭이 그 위에 멈추는지
- 이미 바닥에 붙은 조각의 하드 드롭이 `0`점인지
- 하드 드롭으로 한 줄을 지웠을 때 `score`가 `2 × d + 100`인지
- `GAME_OVER` 상태에 `softDrop()`, `hardDrop()`을 불러도 같은 값인지
- `startGame()` 후 `score`와 `lines`가 둘 다 `0`인지

## 8. 수동 검증

브라우저 콘솔에서 생성기를 고정한 뒤 시작한다.

```js
globalThis.TETRIS_RANDOMIZER = function () { return 'I'; };
```

1. `index.html`을 열고 콘솔 오류가 없는지 확인한다.
2. `시작`을 누르고 `점수:`가 `0`인지 확인한다.
3. `↓`를 다섯 번 눌러 `점수:`가 `5`가 되는지 확인한다.
4. 아무 키도 누르지 않고 두 틱을 기다려 `점수:`가 오르지 않는지 확인한다.
5. `Space`를 눌러 조각이 아래 끝까지 한 번에 내려가는지 확인한다.
6. 그때 `점수:`가 내려간 칸 수의 두 배만큼 올랐는지 확인한다.
7. 마우스로 `시작` 버튼을 한 번 클릭해 포커스를 준 뒤 `Space`를 눌러, 게임이 재시작되지
   않고 하드 드롭만 일어나는지 확인한다.
8. `I` 조각을 눕혀 19행을 채우고 줄이 지워질 때 `점수:`가 `100` 이상 오르는지 확인한다.
9. `제거한 줄:`도 함께 오르는지 확인한다.
10. `시작` 버튼을 눌러 `점수:`와 `제거한 줄:`이 둘 다 `0`으로 돌아가는지 확인한다.
11. `test.html`을 열어 통과·실패 개수를 확인한다.

## 9. 안전과 정지 조건

- 프로젝트 폴더 밖 파일을 만들거나 수정하지 않는다.
- 외부 배포와 유료 서비스를 사용하지 않는다.
- 같은 실패가 두 번 반복되면 중단한다.
- 이 문서에 없는 화면·기술 결정을 해야 하면 추측하지 않고 `[사람 확인 필요]`로 보고한다.
  단 색상·여백 같은 순수 시각 속성은 `MEMORY.md`의 D1에 따라 구현자가 정한다.

### 이 SPEC 고유의 항목

- **SPEC_02에서 무효가 되는 항목.** `/spec-run`은 회차 1에서 `MEMORY.md`에 기록해야 한다.
  실패로 기록하지 않는다.
  1. SPEC_02 완료 조건 23 (AUTO, 통과) "`score`가 어떤 경우에도 `0`을 유지한다"
     → SPEC_03이 점수를 도입하므로 **대체**한다.
  2. SPEC_02 §5와 SPEC_01 §5의 범위 밖 항목 "점수 계산", "하드 드롭(Space)"
     → SPEC_03이 구현한다. 후속 SPEC이 범위를 넓히는 정상적인 진행이다.
- **`↓` 처리 경로가 바뀐다.** `main.js`는 `↓`를 `stepDown()`이 아니라 `softDrop()`으로
  보낸다. 자동 낙하만 `stepDown()`을 쓴다. 둘을 합치면 자동 낙하에 점수가 붙는다.
- **`Space` 기본 동작 차단.** `preventDefault()`를 부르지 않으면 시작 버튼에 포커스가
  있을 때 Space가 버튼을 눌러 게임이 재시작된다. 완료 조건으로도 확인한다.
- **하드 드롭 무한 루프 금지.** `hardDrop()`은 더 내려갈 수 없는 행을 찾는 반복이
  최대 `BOARD_HEIGHT`번 안에 끝나야 한다. 조건이 어긋나 무한히 도는 구현을 만들지 않는다.
- **점수 오버플로 걱정 없음.** 이 단계의 점수 규모로는 정수 정밀도 문제가 생기지 않는다.
  별도 상한을 두지 않는다.
- **회차 예산 경고.** 완료 조건 27개, HUMAN 7건이다. 회차 3을 마친 시점에 `미확인` 또는
  `실패`가 4건 이상이면 이 SPEC을 나누자고 사용자에게 제안하고 정지한다.
- **확인 백로그.** 앞선 SPEC의 미확인 31건이 남아 있다. SPEC_03 회차 1의 사람 확인
  요청은 이것들과 합쳐서 한 번에 낸다.
